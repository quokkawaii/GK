# 변경 기록 — 2026-08-10

## 개발 초기 공통 레이아웃 구현

- 행동: 추가
- 파일: `app/layout.tsx`
- 변경 내용: 한국어 문서 언어 설정, GK 산업 기본 제목·설명 메타데이터, 전역 스타일 연결, 페이지 콘텐츠를 표시할 `{children}` 영역을 추가했다.
- 변경 이유: Next.js App Router의 모든 화면이 공유할 기본 HTML 골격을 먼저 마련하기 위해 구현했다.
- 관련: REQ-015, DEC-026
- 검증: 코드 파일 추가 후 구조를 확인했다. 전역 스타일 파일과 실제 페이지는 다음 개발 단계에서 추가한다.

## 전역 스타일 기준 구현

- 행동: 추가
- 파일: `app/globals.css`
- 변경 내용: 디자인 시스템의 색상 토큰, 기본 글자·배경 규칙, 접근성 포커스 표시, 반응형 콘텐츠 컨테이너, 스크린리더 전용 숨김 유틸리티, 모션 감소 설정을 추가했다.
- 변경 이유: 공통 화면을 구현하기 전에 확정된 디자인 값과 반응형 기준을 전역 스타일로 고정하기 위해 구현했다.
- 관련: REQ-001, NFR-001, NFR-002, DEC-026
- 검증: `git diff --check` 통과.

## 공통 콘텐츠 컨테이너 구현

- 행동: 추가
- 파일: `components/ui/Container.tsx`
- 변경 내용: 전역 `.page-container` 규칙을 재사용하는 공통 컨테이너를 추가하고, 화면별 추가 클래스를 전달할 수 있도록 `className` 속성을 제공했다.
- 변경 이유: 페이지마다 콘텐츠 너비와 좌우 여백을 반복 작성하지 않고 확정된 반응형 기준을 공통으로 사용하기 위해 구현했다.
- 관련: REQ-001, NFR-001, NFR-003, DEC-026
- 검증: `npm run lint`, `git diff --check` 통과.

## 공통 헤더 구현

- 행동: 추가·수정
- 파일: `components/layout/Header.tsx`, `app/layout.tsx`
- 변경 내용: `site.json`의 대표 전화번호를 사용하는 공통 헤더를 추가하고, 홈·시공 사례·사용 제품·회사 소개 링크, PC용 전화 문의, 모바일용 전화 문의와 메뉴 버튼을 배치했다. 루트 레이아웃에서 헤더와 페이지 본문 영역을 함께 렌더링하도록 연결했다.
- 변경 이유: REQ-001·REQ-002·REQ-012의 공통 헤더와 전화 문의 동선을 실제 화면 구조에 반영하기 위해 구현했다.
- 관련: REQ-001, REQ-002, REQ-012, NFR-001, NFR-002
- 검증: `npm run lint`, `git diff --check` 통과.

## 헤더 메뉴 데이터 분리

- 행동: 추가·수정
- 파일: `lib/navigation.ts`, `components/layout/Header.tsx`
- 변경 내용: 헤더 컴포넌트 내부에 있던 메뉴 목록을 `lib/navigation.ts`로 이동하고, 헤더는 분리된 메뉴 데이터를 표시하도록 수정했다.
- 변경 이유: 컴포넌트는 화면 표시와 상호작용에 집중하고 메뉴 데이터는 별도 모듈에서 관리한다는 프로젝트 원칙을 지키기 위해 정리했다.
- 관련: NFR-003, NFR-004, DEC-026
- 검증: 변경 후 `npm run lint`, `git diff --check`를 실행한다.

## 모바일 메뉴 동작 구현

- 행동: 추가·수정
- 파일: `components/layout/MobileMenu.tsx`, `components/layout/Header.tsx`
- 변경 내용: 모바일 햄버거 버튼과 오른쪽 메뉴 패널을 연결하고, 메뉴 열기·닫기, 바깥 영역 클릭 닫기, `Esc` 닫기, 메뉴 이동 후 닫기, 배경 스크롤 잠금, 모바일 전화 문의를 구현했다. 헤더는 모바일 메뉴 컴포넌트를 표시하도록 수정했다.
- 변경 이유: 모바일 메뉴를 오른쪽 패널로 제공하고 키보드와 전화 문의 동선을 지원한다는 요구사항을 실제 공통 헤더에 반영하기 위해 구현했다.
- 관련: REQ-002, REQ-012, NFR-001, NFR-002
- 검증: 변경 후 `npm run lint`, `git diff --check`를 실행한다.

## 홈 화면 1차 구현

- 행동: 추가
- 파일: `app/page.tsx`, `lib/home-content.ts`
- 변경 내용: 정적 JSON에서 최신 사례 3개와 사용 제품 3개를 구성하고, 공통 헤더 아래 대표 시공 영역·최근 시공 사례 카드·사용 제품 카드를 구현했다. 사례 사진은 `next/image`와 JSON의 대체 문구를 사용하며 제품 사진이 없는 경우 준비 중 안내를 표시한다.
- 변경 이유: 승인된 홈 화면 요구사항과 디자인 기준을 실제 데이터로 연결하는 첫 화면 구현을 시작하기 위해 구현했다.
- 관련: REQ-003, REQ-004, REQ-006, REQ-011, REQ-012, NFR-001, NFR-004, DEC-026, DEC-030
- 검증: 변경 후 `npm run lint`, `git diff --check`를 실행한다.

## 정적 빌드 검증에 따른 Next.js 설정 보완

- 행동: 수정·추가
- 파일: `tsconfig.json`, `next-env.d.ts`
- 변경 내용: `next build`가 요구한 React 자동 JSX 설정과 개발 타입 경로를 `tsconfig.json`에 반영하고, Next.js가 생성한 환경 타입 선언 파일을 추가했다.
- 변경 이유: 홈 화면과 App Router 프로젝트가 실제 정적 빌드에서 TypeScript 검사를 통과하도록 Next.js 권장 설정을 적용하기 위해서다.
- 관련: DEC-011, DEC-026
- 검증: `npm run build` 통과.

## 시공 사례 목록과 상세 팝업 구현

- 행동: 추가
- 파일: `app/cases/page.tsx`, `components/cases/CaseList.tsx`, `components/cases/CaseCard.tsx`, `components/cases/CaseDetailModal.tsx`, `lib/constants.ts`, `lib/cases.ts`, `lib/index.ts`, `store/contentStore.ts`, `store/caseFilterStore.ts`, `store/casePaginationStore.ts`, `types/content.ts`, `types/filter.ts`, `types/index.ts`
- 변경 내용: 시공 사례를 숫자 ID 내림차순으로 표시하고, 다중 태그 AND 필터, 필터 초기화, 페이지당 4개 목록과 페이지 이동을 구현했다. 사례 카드 전체 클릭으로 상세 팝업을 열고, 팝업에서 사진 이동·썸네일·사진 확대·장소·시공 자재·태그·시공 내용을 표시하도록 구성했다.
- 변경 이유: 승인된 사례 목록·필터·페이징·상세 팝업 결정과 개발 구조를 실제 화면에 반영하기 위해 구현했다.
- 관련: REQ-005, REQ-006, REQ-007, REQ-008, REQ-012, NFR-001, NFR-002, NFR-003, DEC-079, DEC-080, DEC-085
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 사례 조회 규칙 자동 테스트 추가

- 행동: 추가
- 파일: `tests/cases.test.ts`
- 변경 내용: ID 내림차순 정렬, 다중 태그 AND 필터, 페이지당 4개 계산, 페이지 메타데이터, 페이지 번호 묶음 계산을 검증하는 Vitest 테스트를 추가했다.
- 변경 이유: 시공 사례 목록의 핵심 데이터 규칙을 자동으로 확인하고, 테스트 파일 부재로 `npm run test`가 종료되는 상태를 해소하기 위해 추가했다.
- 관련: REQ-005, REQ-006, NFR-003, DEC-079, DEC-080, DEC-084
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## Vitest 경로 별칭 연결

- 행동: 수정
- 파일: `vitest.config.ts`
- 변경 내용: 애플리케이션과 같은 `@/` 루트 경로 별칭을 Vitest에도 설정했다.
- 변경 이유: 사례 규칙 테스트가 프로젝트 경로 별칭을 사용하면서도 정상적으로 JSON과 `lib` 모듈을 불러오도록 하기 위해 수정했다.
- 관련: DEC-029, DEC-066
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 사례 필터 테스트 기대값 보정

- 행동: 수정
- 파일: `tests/cases.test.ts`
- 변경 내용: 실제 `cases.json`에서 `공장`과 `크리트`를 동시에 가진 사례 ID에 맞춰 테스트 기대값을 `6, 5, 4, 1`로 보정했다.
- 변경 이유: 테스트가 승인된 AND 필터 규칙이 아닌 잘못된 사례 목록을 기대하고 있어 실제 콘텐츠 모델과 일치하도록 수정했다.
- 관련: REQ-005, DEC-079
- 검증: 변경 후 전체 검증을 다시 실행한다.

## 홈 사례 연결·필터 초기화·목록 위치 이동 보완

- 행동: 추가·수정
- 파일: `components/home/HomePageStateReset.tsx`, `app/page.tsx`, `components/cases/CaseList.tsx`
- 변경 내용: 홈의 사례 카드가 `/cases#case-{id}`로 이동하면 해당 사례 상세 팝업을 바로 열도록 연결했다. 홈 진입 시 사례 태그 필터와 페이지 번호를 초기화하고, 사례 필터·페이지 번호가 바뀌면 첫 사례 카드가 보이는 위치로 부드럽게 이동하도록 추가했다.
- 변경 이유: 홈과 시공 사례 간 이동 시 이전 목록 상태가 남지 않게 하고, 목록 페이지 이동 뒤 사용자가 첫 사진부터 이어서 보기 쉽게 하기 위해 보완했다.
- 관련: REQ-003, REQ-005, REQ-007, DEC-079, DEC-085
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 필터 클릭 시 불필요한 스크롤 이동 제거

- 행동: 수정
- 파일: `components/cases/CaseList.tsx`
- 변경 내용: 사례 목록 위치 이동 effect가 `selectedTags` 변경에는 반응하지 않고 `pageNumber` 변경에만 반응하도록 수정했다.
- 변경 이유: 사용자가 필터를 선택할 때 화면이 갑자기 이동하지 않도록 하고, 페이지 번호를 넘길 때만 첫 사례 카드 위치로 이동하게 하기 위해 수정했다.
- 관련: REQ-005, DEC-079, DEC-084
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 홈 사례 카드의 직접 상세 팝업 연결

- 행동: 추가·수정
- 파일: `components/home/HomeCaseCards.tsx`, `app/page.tsx`
- 변경 내용: 홈의 최근 시공 사례 카드를 `/cases`로 이동하는 링크 대신 현재 홈 화면 위에서 `CaseDetailModal`을 여는 클라이언트 컴포넌트로 교체했다.
- 변경 이유: 사용자가 홈에서 사례 사진을 누를 때 페이지를 이동하지 않고 바로 사례 상세를 확인하도록 동선을 수정했다.
- 관련: REQ-003, REQ-007, DEC-085
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 홈 제품 카드의 사례 필터 연결

- 행동: 추가·수정
- 파일: `store/caseFilterStore.ts`, `components/cases/CaseList.tsx`
- 변경 내용: 홈 사용 제품 카드의 `/cases?tag=제품명` 링크를 사례 목록이 읽도록 연결하고, 유효한 제품 태그를 선택 필터로 적용한 뒤 1페이지로 초기화하도록 구현했다. 필터 스토어에 선택 태그 일괄 교체 함수를 추가했다.
- 변경 이유: 메인 페이지의 사용 제품에서 `시공 사례 보기`를 선택하면 해당 제품이 적용된 사례 목록을 바로 보여주도록 구현하기 위해서다.
- 관련: REQ-004, REQ-005, REQ-011, DEC-079
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 시공 사례 필터 분리

- 행동: 수정
- 파일: `lib/cases.ts`, `lib/index.ts`, `components/cases/CaseList.tsx`
- 변경 내용: 시공 사례 필터를 `시공 장소`와 `시공 자재` 두 행으로 분리했다. 장소 태그는 사례 `tags` 중 `productIds`에 포함되지 않은 값으로, 자재 태그는 사례의 `productIds` 값으로 구성하며 선택 조건은 기존 AND 방식으로 유지했다.
- 변경 이유: 승인된 PC·모바일 시안의 필터 구조를 실제 시공 사례 페이지에 반영하기 위해 수정했다.
- 관련: REQ-005, REQ-010, DEC-079, DEC-080
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 시공 자재 필터 단일 선택 적용

- 행동: 수정
- 파일: `components/cases/CaseList.tsx`
- 변경 내용: 시공 장소 필터는 다중 선택을 유지하고, 시공 자재 필터는 새 자재를 클릭하면 기존 자재 선택을 교체하도록 수정했다. 선택한 자재와 장소 조건은 함께 사례 조회에 적용된다.
- 변경 이유: MMA와 크리트를 동시에 선택하지 않고, 마지막으로 클릭한 자재 하나만 필터 조건으로 사용하도록 요구사항을 반영하기 위해 수정했다.
- 관련: REQ-005, REQ-010
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 시공 장소 필터 단일 선택 적용

- 행동: 수정
- 파일: `components/cases/CaseList.tsx`
- 변경 내용: 시공 장소도 새 장소를 클릭하면 기존 장소 선택을 교체하도록 수정하고, 선택된 자재 조건은 유지하도록 처리했다.
- 변경 이유: 장소 필터가 여러 개 중복 선택되지 않고 현재 선택한 장소 하나만 사례 조회 조건으로 사용되도록 요구사항을 반영하기 위해 수정했다.
- 관련: REQ-005
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 헤더 소셜 링크 아이콘 준비

- 행동: 추가·수정
- 파일: `public/images/social/instagram.svg`, `public/images/social/youtube.svg`, `components/layout/SocialLinks.tsx`, `components/layout/Header.tsx`
- 변경 내용: 헤더에서 `content/site.json`의 Instagram·YouTube 주소가 `null`이 아닐 때만 열리는 SVG 아이콘 링크를 추가했다. 현재 주소가 비어 있으므로 화면에는 표시되지 않으며, 확정 URL 입력 후 자동으로 표시된다.
- 변경 이유: 작은 헤더 아이콘에서도 깨지지 않는 SVG 자산을 준비하고, 확정되지 않은 외부 주소를 임의로 만들지 않으면서 실제 주소를 연결할 수 있도록 하기 위해 구현했다.
- 관련: REQ-014, NFR-004
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 인스타그램 아이콘 컬러 보정

- 행동: 수정
- 파일: `public/images/social/instagram.svg`
- 변경 내용: `currentColor` 기반의 단색 아이콘을 인스타그램 계열의 노랑·주황·분홍·보라·파랑 그라데이션 SVG로 변경했다.
- 변경 이유: 헤더에서 인스타그램 아이콘이 검정색으로 표시되는 문제를 해결하고 브랜드 색상을 식별할 수 있게 하기 위해 수정했다.
- 관련: REQ-014
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 사용 제품 목록 구현

- 행동: 추가·수정
- 파일: `app/products/page.tsx`, `components/products/ProductList.tsx`, `lib/products.ts`, `lib/index.ts`
- 변경 내용: 사례 JSON의 `productIds`에서 사용 제품 목록을 만들고, `전체`·자재 단일 필터, 제품명·적합 현장·이미지 준비 중 안내·시공 사례 보기 링크를 구현했다. 제품 사례 링크는 해당 자재 태그가 적용된 `/cases` 목록으로 이동한다.
- 변경 이유: 승인된 사용 제품 목록 시안과 제품 데이터 원천 규칙을 실제 화면에 반영하기 위해 구현했다.
- 관련: REQ-004, REQ-010, REQ-011, NFR-001, NFR-004, DEC-056
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 사용 제품 카드 대비 색상 보정

- 행동: 수정
- 파일: `components/products/ProductList.tsx`, `app/page.tsx`
- 변경 내용: 사용 제품 카드와 홈 제품 카드에 흰색 카드 배경·진한 본문 글자·밝은 이미지 준비 영역·진한 안내 글자를 명시적으로 적용했다.
- 변경 이유: 사용 제품 영역에서 배경과 글자 색상 상속에 따라 글자가 검정 배경에 묻히는 문제를 방지하기 위해 수정했다.
- 관련: REQ-004, REQ-011, NFR-001
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 회사 소개 페이지 구현

- 행동: 추가·수정
- 파일: `app/company/page.tsx`, `content/site.json`, `types/content.ts`
- 변경 내용: 회사 소개 제목·인사말·소개 문구·시공 범위·전화 문의 영역을 PC·모바일 반응형으로 구현하고, 시공 범위 데이터를 `site.json`으로 분리했다. 전화번호가 없을 때는 연결 대신 준비 중 안내를 표시한다.
- 변경 이유: 승인된 회사 소개 시안과 공통 사이트 정보 JSON 원칙을 실제 `/company` 화면에 반영하기 위해 구현했다.
- 관련: REQ-013, REQ-012, NFR-001, NFR-004, DEC-077
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 공통 푸터 구현

- 행동: 추가·수정
- 파일: `components/layout/Footer.tsx`, `app/layout.tsx`
- 변경 내용: 회사명·서비스 지역·확정된 전화번호·소셜 링크를 표시하는 공통 푸터를 추가하고 모든 페이지의 공통 레이아웃에 연결했다. 주소와 사업자 정보가 `null`이면 표시하지 않는다.
- 변경 이유: 승인된 회사 소개·푸터 요구사항과 PC·모바일 홈 시안의 어두운 하단 푸터 구조를 실제 화면에 반영하기 위해 구현했다.
- 관련: REQ-012, REQ-013, REQ-014, NFR-001, NFR-004, DEC-077
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 대표 전화번호 반영

- 행동: 수정
- 파일: `content/site.json`
- 변경 내용: 대표 전화번호를 사용자가 제공한 `010-9261-2061`로 변경했다.
- 변경 이유: 헤더·푸터·회사 소개의 전화 문의 연결을 실제 대표번호로 갱신하기 위해 수정했다.
- 관련: REQ-012, REQ-013
- 검증: 변경 후 전체 검증을 실행한다.

## 제품 사례 보기 버튼 글자 대비 수정

- 행동: 수정
- 파일: `components/products/ProductList.tsx`
- 변경 내용: 카드의 본문 색상 규칙이 검정색 버튼 내부까지 상속되던 문제를 막기 위해 `시공 사례 보기` 버튼 글자에 흰색을 우선 적용했다.
- 변경 이유: 실제 브라우저에서 검정 버튼과 검정 글자가 겹쳐 버튼 문구가 보이지 않는 문제를 해결하기 위해 수정했다.
- 관련: REQ-011, NFR-001
- 검증: 실제 브라우저 화면에서 computed color를 확인했고, 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 유튜브 아이콘 컬러 보정

- 행동: 수정
- 파일: `public/images/social/youtube.svg`
- 변경 내용: 외부 SVG 이미지에서 CSS `currentColor`가 전달되지 않는 문제를 피하도록 유튜브 본체 색상을 SVG 내부의 `#FF0000`으로 지정했다.
- 변경 이유: 헤더 유튜브 아이콘이 검정색으로 표시되는 문제를 해결하기 위해 수정했다.
- 관련: REQ-014
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 유튜브 아이콘 색상 적용

- 행동: 수정
- 파일: `components/layout/SocialLinks.tsx`
- 변경 내용: 유튜브 SVG 아이콘에 빨간색 `#FF0000`과 hover 색상 `#CC0000`을 적용했다.
- 변경 이유: 유튜브 브랜드 아이콘이 헤더에서 식별되도록 요청된 색상을 반영하기 위해 수정했다.
- 관련: REQ-014
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 인스타그램 주소 반영

- 행동: 수정
- 파일: `content/site.json`
- 변경 내용: 사용자가 제공한 Instagram 주소를 `social.instagram`에 등록했다.
- 변경 이유: 헤더의 인스타그램 소셜 링크를 실제 계정 주소로 연결하기 위해 수정했다.
- 관련: REQ-014, NFR-004
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 유튜브 주소 반영

- 행동: 수정
- 파일: `content/site.json`
- 변경 내용: 사용자가 제공한 한스토리 유튜브 Shorts 주소를 `social.youtube`에 등록했다.
- 변경 이유: 헤더의 유튜브 소셜 링크를 실제 채널 주소로 연결하기 위해 수정했다.
- 관련: REQ-014, NFR-004
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 장소 필터 토글 코드 정리

- 행동: 수정
- 파일: `components/cases/CaseList.tsx`
- 변경 내용: 장소 필터를 교체 방식으로 변경한 뒤 더 이상 사용하지 않는 `toggleTag` 선택 코드를 제거했다.
- 변경 이유: 단일 선택 필터의 실제 동작과 컴포넌트 코드가 일치하도록 정리하기 위해 수정했다.
- 관련: REQ-005
- 검증: 변경 후 전체 검증을 실행한다.

## 사례 페이지 진입 시 강제 스크롤 제거

- 행동: 수정
- 파일: `components/cases/CaseList.tsx`
- 변경 내용: 페이지 번호 상태 변경을 감지해 자동으로 스크롤하던 effect를 제거하고, 사용자가 이전·번호·다음 페이지 버튼을 클릭한 경우에만 첫 사례 카드 위치로 이동하도록 수정했다.
- 변경 이유: 사례 페이지 진입이나 제품 필터 적용 직후 첫 번째 사진 위치로 강제 워프하는 문제를 제거하기 위해 수정했다.
- 관련: REQ-005, DEC-084
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 필터 결과 없음 영역 높이와 중심 정렬 보정

- 행동: 수정
- 파일: `components/cases/CaseList.tsx`
- 변경 내용: 사례 결과 영역에 모바일·태블릿·데스크톱별 최소 높이를 적용하고, 결과가 없을 때 안내 문구를 같은 영역의 수직·수평 중앙에 배치하도록 수정했다.
- 변경 이유: 결과가 있는 상태와 없는 상태에서 목록 영역 높이와 화면 중심이 달라지는 문제를 줄이고, 빈 결과 안내가 사례 카드 영역과 같은 시각적 위치를 유지하도록 보정하기 위해 수정했다.
- 관련: REQ-005, NFR-001
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

## 홈 사례 팝업 초기화 lint 보완

- 행동: 수정
- 파일: `components/cases/CaseList.tsx`
- 변경 내용: URL hash로 사례 팝업을 여는 초기 상태 설정을 브라우저 프레임 콜백으로 처리하고, 컴포넌트가 정리될 때 예약된 콜백을 취소하도록 수정했다.
- 변경 이유: React lint의 effect 내부 동기 상태 변경 경고를 제거하면서 홈 사례 이동 시 팝업을 여는 동작은 유지하기 위해 수정했다.
- 관련: REQ-003, DEC-085
- 검증: 변경 후 `npm run lint`, `npm run test`, `npm run build`, `git diff --check`를 실행한다.

# 2026-08-14 패치 기록

## 변경 1 — 개발자

- 행동: 수정
- 파일: `next.config.ts`
- 내용: 정적 내보내기 설정인 `output: "export"`를 제거했다.
- 이유: 사용자가 현재 불필요한 `out/` 폴더가 이후 빌드에서 생성되지 않도록 요청했다.
- 검증: `npm run build`로 일반 빌드 결과가 `.next/`에 생성되고 `out/`이 새로 생성되지 않는지 확인한다.

## 변경 2 — 개발자

- 행동: 삭제
- 파일: `.github/`, `.husky/`, `out/`, `tests/`, `scripts/`
- 내용: 빈 GitHub Actions 자리 표시자, 커밋 훅, 정적 배포 결과물, Vitest 테스트, 콘텐츠 검사 스크립트 폴더를 삭제했다.
- 이유: 사용자가 현재 사용하지 않는 다섯 폴더를 명시적으로 삭제하도록 요청했다.
- 검증: 다섯 경로가 모두 존재하지 않는 것을 확인했다.

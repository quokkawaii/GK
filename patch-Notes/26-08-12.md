# 변경 1 — 리뷰 이슈 관리 규칙 정리

- 행동: 수정
- 파일: `AGENTS.md`, `harness/roles/_shared.md`, `harness/roles/reviewer.md`, `harness/roles/developer.md`, `docs/development/project-structure.md`
- 내용: 기획·디자인·개발별 독립 이슈 ID, 이슈 발생일 폴더, 중앙 미해결 목록, 원본 이슈 파일에서의 해결 기록 규칙을 추가했다.
- 이유: 사용자가 지정한 REV 관리 방식으로 흩어진 이슈와 중복 번호 문제를 정리하기 위해서다.
- 관련: PLAN-REV / DESIGN-REV / DEV-REV
- 검증: 규칙 문서와 폴더 구조 설명이 같은 파일명·해결 절차를 가리키는지 확인했다.

# 변경 2 — 과거 리뷰 이력의 날짜별 이전 (1차)

- 행동: 추가
- 파일: `harness/review/issue-register.md`, `harness/review/26-08-07-issue/26-08-07-plan-issues.md`, `harness/review/26-08-08-issue/26-08-08-design-issues.md`, `harness/review/26-08-08-issue/26-08-08-dev-issues.md`
- 내용: 과거 기획·디자인·개발 이슈를 영역별 독립 ID와 날짜별 이력 파일로 보존하고, 중앙 목록의 형식을 만들었다.
- 이유: 사용자가 요청한 날짜·영역별 이슈 관리 구조로 기존 전역 REV 기록을 이전하기 위해서다.
- 관련: PLAN-REV-001 ~ PLAN-REV-012, DESIGN-REV-001 ~ DESIGN-REV-008, DEV-REV-001 ~ DEV-REV-002
- 검증: 해결된 과거 항목은 중앙 목록에 넣지 않고 각 날짜 파일에 해결 방법·검증 결과를 기록했다.

# 변경 3 — 현재 미해결 개발 이슈 등록 및 과거 이력 이전 (2차)

- 행동: 추가
- 파일: `harness/review/26-08-09-issue/26-08-09-design-issues.md`, `harness/review/26-08-09-issue/26-08-09-dev-issues.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 2026-08-09의 디자인·개발 이력을 이전하고, 현재 코드 리뷰에서 확인된 DEV-REV-004 ~ DEV-REV-009를 발생일 파일에 기록했다.
- 이유: 해결된 과거 이력과 현재 미해결 이슈를 같은 규칙으로 조회할 수 있게 하기 위해서다.
- 관련: DESIGN-REV-009 ~ DESIGN-REV-010, DEV-REV-003 ~ DEV-REV-009
- 검증: `issue-register.md`의 모든 미해결 이슈가 26-08-12 개발 이슈 파일에 상세 내용으로 연결됨을 확인했다.

# 변경 4 — 이전 전역 REV 리뷰 파일 제거

- 행동: 삭제
- 파일: `harness/review/review-log.md`, `harness/review/design-review-2026-08-08.md`, `harness/review/design-system-review-2026-08-08.md`, `harness/review/development-design-review-2026-08-08.md`, `harness/review/popup-design-review-2026-08-09.md`, `harness/review/development-structure-review-2026-08-09.md`
- 내용: 기존의 전역 `REV-` 번호와 단일 리뷰 로그 기반 파일을 제거했다.
- 이유: 같은 이력을 이미 날짜·영역별 파일에 독립 ID와 해결 기록으로 이전해, 두 이력 체계가 함께 남아 혼동되는 것을 막기 위해서다.
- 관련: PLAN-REV-001 ~ PLAN-REV-012, DESIGN-REV-001 ~ DESIGN-REV-010, DEV-REV-001 ~ DEV-REV-009
- 검증: `harness/review/` 루트에는 `issue-register.md`와 이슈 발생일 폴더만 남는다.

# 변경 5 — 과거 기획 이슈의 개별 식별 정리

- 행동: 수정
- 파일: `harness/review/26-08-07-issue/26-08-07-plan-issues.md`
- 내용: 묶음으로 기록한 PLAN-REV-001 ~ PLAN-REV-012를 각 이슈 번호별 독립 항목으로 나누고, 해결 방법·수정 파일·검증 결과를 각각 기록했다.
- 이유: 사용자가 특정 이슈 번호를 찾아 해결 내용을 바로 확인할 수 있게 하기 위해서다.
- 관련: PLAN-REV-001 ~ PLAN-REV-012
- 검증: 모든 기획 이슈 번호가 하나의 제목과 해결 상태를 갖는지 확인했다.

# 변경 6 — 과거 디자인 이슈의 개별 식별 정리

- 행동: 수정
- 파일: `harness/review/26-08-08-issue/26-08-08-design-issues.md`
- 내용: 묶음으로 기록한 DESIGN-REV-001 ~ DESIGN-REV-008을 각 이슈 번호별 독립 항목으로 나누고, 해결 방법·수정 파일·검증 결과를 각각 기록했다.
- 이유: 사용자가 특정 이슈 번호를 찾아 해결 내용을 바로 확인할 수 있게 하기 위해서다.
- 관련: DESIGN-REV-001 ~ DESIGN-REV-008
- 검증: 모든 디자인 이슈 번호가 하나의 제목과 해결 상태를 갖는지 확인했다.

# 변경 7 — 누락된 개발 리뷰 이슈 등록

- 행동: 수정
- 파일: `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 중복 제품 조회, 회사 소개 배열 위치 의존, 대표 제품 선정 규칙, 전화번호 null 처리, 음수 페이지 번호, robots·sitemap 누락을 DEV-REV-010 ~ DEV-REV-015로 등록했다.
- 이유: 이전 리뷰와 현재 소스를 재대조한 결과 누락된 미해결 이슈를 중앙 목록과 원본 이슈 파일에 반영하기 위해서다.
- 관련: DEV-REV-010 ~ DEV-REV-015
- 검증: 각 이슈가 중앙 목록의 한 줄 요약과 날짜 파일의 대상·근거·영향·권장 조치로 연결됨을 확인했다.

# 변경 8 — 사용자 질문의 구체성 규칙 추가

- 행동: 수정
- 파일: `AGENTS.md`, `harness/roles/_shared.md`
- 내용: 인터뷰·질문·설명에서 내부 구현 개념으로 사용자의 판단을 추상화하지 않고, 현재 화면·콘텐츠·선택지와 결정 결과를 구체적으로 제시하도록 규칙을 추가했다.
- 이유: 사용자가 실제 선택할 항목을 바로 판단할 수 있는 질문 방식을 요청했기 때문이다.
- 관련: 없음
- 검증: 공통 규칙과 역할 공통 계약에 같은 사용자 소통 원칙이 기록됐는지 확인했다.

# 변경 9 — 홈 제품 표시 순서 결정 기록

- 행동: 수정
- 파일: `docs/requirements/implementation-decisions.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 홈 사용 제품은 별도 대표 목록 없이 제품명을 정렬한 뒤 앞에서부터 최대 3개를 표시하도록 DEC-086을 기록했다. 회사 소개 배열 이슈의 권장 조치에서 홈 소개 문구 필드를 제거했다.
- 이유: 사용자가 홈에는 회사 소개를 넣지 않는 기존 결정을 재확인했고, 제품 표시 순서를 정렬 결과로 정했기 때문이다.
- 관련: DEC-086, DEV-REV-011, DEV-REV-012
- 검증: 결정 기록과 DEV-REV-012의 구현 기준이 같은 정렬·최대 3개 규칙을 가리키는지 확인했다.

# 변경 10 — 사이트 도메인 미확정 상태 기록

- 행동: 수정
- 파일: `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: DEV-REV-015에 실제 배포 도메인이 아직 정해지지 않았다는 사용자 확인과 도메인 확정 대기 상태를 기록했다.
- 이유: 사용자가 현재 도메인을 정하지 않고 이슈를 유지하도록 요청했기 때문이다.
- 관련: DEV-REV-015
- 검증: 중앙 미해결 목록은 유지되고, 상세 이슈에만 대기 사유가 기록됐는지 확인했다.

# 변경 11 — 잘못 등록한 전화번호 이슈 철회

- 행동: 수정
- 파일: `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: DEV-REV-013을 중앙 미해결 목록에서 제거하고, 실제 Git 추적값에 전화번호가 존재한다는 철회 사유를 원본 이슈에 기록했다.
- 이유: 사용자가 제공한 전화번호가 `src/content/site.json`에 이미 있으며, 전화번호를 비워 둘 요구사항이 없는데 타입만 보고 가정한 잘못된 리뷰 지적이었기 때문이다.
- 관련: DEV-REV-013
- 검증: `git show HEAD:src/content/site.json`에서 `phone: "010-9261-2061"`을 확인했다.

# 변경 12 — 이슈 처리 연속 진행 규칙 추가

- 행동: 수정
- 파일: `AGENTS.md`, `harness/roles/reviewer.md`
- 내용: 이슈를 해결·철회·대기 상태로 기록한 직후 다음 미해결 이슈의 검토 또는 인터뷰로 바로 이어가도록 규칙을 추가했다.
- 이유: 사용자가 이슈 처리 후 상태 보고만 하지 말고 다음 이슈로 즉시 진행하도록 요청했기 때문이다.
- 관련: 없음
- 검증: 공통 규칙과 리뷰어 역할 지침에 같은 연속 진행 조건이 기록됐는지 확인했다.

# 변경 13 — 잘못된 페이지 번호 처리 기준 기록

- 행동: 수정
- 파일: `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: DEV-REV-014의 현재 페이지를 1부터 저장하고, 코드에서 0 이하 값을 전달하면 현재 페이지를 유지하도록 결정했다.
- 이유: 사용자가 음수 페이지를 직접 입력하는 상황을 가정하지 않고, 코드의 잘못된 값 전달만 간단히 방어하도록 요청했기 때문이다.
- 관련: DEV-REV-014
- 검증: 이슈의 문제·결정·권장 조치가 모두 1부터 시작하는 페이지와 0 이하 입력 무시 규칙을 가리키는지 확인했다.

# 변경 14 — 최대 페이지 초과 처리 기준 보완

- 행동: 수정
- 파일: `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: DEV-REV-014에서 현재 전체 페이지 수보다 큰 값도 현재 페이지를 유지하도록 방어 범위를 확장했다.
- 이유: 사용자가 0 이하뿐 아니라 최대 페이지보다 큰 값도 코드에서 전달될 수 있음을 확인했기 때문이다.
- 관련: DEV-REV-014
- 검증: 이슈의 결정과 권장 조치가 유효 범위 밖의 모든 페이지 번호를 현재 페이지 유지로 처리하도록 기록됐는지 확인했다.

# 변경 15 — 사례 목록 화면 분리 범위 기록

- 행동: 수정
- 파일: `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: DEV-REV-007에서 필터 영역과 페이지 번호 영역만 각각 `CaseFilters.tsx`, `CasePagination.tsx`로 분리하고, 나머지 상태와 화면 조립은 `CaseList.tsx`에 유지하도록 기록했다.
- 이유: 사용자가 분리는 개발 단계에서 진행하되, 불필요한 커스텀 훅이나 상태 구조 변경 없이 화면 영역만 나누도록 결정했기 때문이다.
- 관련: DEV-REV-007
- 검증: 이슈의 결정과 권장 조치가 같은 파일 분리 범위를 가리키는지 확인했다.

# 변경 16 — 페이지·콘텐츠 모델·제품 조회 기준 수정

- 행동: 수정
- 파일: `src/lib/constants.ts`, `src/features/cases/store/casePaginationStore.ts`, `src/lib/cases.ts`, `src/lib/home-content.ts`, `src/lib/products.ts` 사용 경로, `src/app/page.tsx`, `src/types/content.ts`, `src/app/company/page.tsx`, `src/content/site.json`
- 내용: 페이지 번호를 1부터 관리하고 유효 범위 밖의 낮은 값은 저장하지 않도록 했으며, 홈 제품을 정렬 후 최대 3개로 계산했다. 홈·제품 화면의 제품 조회를 공통 함수로 연결하고 회사 소개 배열을 의미 있는 필드로 바꿨다.
- 이유: DEV-REV-010, DEV-REV-011, DEV-REV-012, DEV-REV-014와 사용자가 확정한 페이지·콘텐츠 기준을 반영하기 위해서다.
- 관련: DEV-REV-010, DEV-REV-011, DEV-REV-012, DEV-REV-014, DEC-086
- 검증: 후속 테스트에서 페이지 경계·정렬·콘텐츠 형태를 확인할 예정이다.

# 변경 17 — 테스트 데이터 독립화

- 행동: 수정
- 파일: `tests/cases.test.ts`
- 내용: 실제 `src/content/cases.json`의 ID·개수 대신 테스트 파일 안의 작은 사례 데이터를 사용하도록 변경하고 페이지 번호 기준을 1부터 맞췄다.
- 이유: DEV-REV-009에 따라 콘텐츠를 추가·교체해도 조회 로직 테스트가 데이터 변경 때문에 실패하지 않게 하기 위해서다.
- 관련: DEV-REV-009, DEV-REV-014
- 검증: Vitest 실행 예정.

# 변경 18 — 사례 목록 화면 컴포넌트 분리

- 행동: 추가·수정
- 파일: `src/features/cases/components/CaseFilters.tsx`, `src/features/cases/components/CasePagination.tsx`, `src/features/cases/components/CaseList.tsx`
- 내용: 필터 UI와 페이지 번호 UI를 각각 분리하고 `CaseList`는 데이터·상태·화면 조립을 담당하도록 정리했다.
- 이유: DEV-REV-007과 사용자가 확정한 개발 단계 분리 범위를 반영하기 위해서다.
- 관련: DEV-REV-007
- 검증: lint·format·test·build 실행 예정.

# 변경 19 — 정적 콘텐츠 검증 명령 추가

- 행동: 추가·수정
- 파일: `scripts/check-content.mjs`, `package.json`, `docs/development/project-structure.md`
- 내용: 사례 ID 중복, 필수 문자열, 비어 있는 제품·이미지 배열, `thumbnailIndex` 범위, 회사 소개 필드를 검사하는 `npm run check:content` 명령을 추가했다.
- 이유: DEV-REV-008과 DEC-032에 따라 잘못된 콘텐츠를 빌드 전에 발견하기 위해서다.
- 관련: DEV-REV-008, DEC-032
- 검증: 콘텐츠 검증 명령 실행 예정.

# 변경 20 — 개발 변경 파일 서식 정리

- 행동: 수정
- 파일: 이번 개발 변경으로 수정된 `package.json`, `docs/development/project-structure.md`, `src/`, `tests/cases.test.ts`
- 내용: Prettier로 이번 변경 파일의 서식을 프로젝트 설정에 맞췄다.
- 이유: 개발 변경 후 일관된 코드 서식과 포맷 검사 통과를 확인하기 위해서다.
- 관련: DEV-REV-004 ~ DEV-REV-012, DEV-REV-014
- 검증: 변경 파일 대상 Prettier 실행 완료.

# 변경 21 — 사용 제품 다중 AND 필터 구현

- 행동: 수정
- 파일: `src/lib/products.ts`, `src/lib/index.ts`, `src/features/products/components/ProductList.tsx`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 제품 선택 상태를 배열로 바꾸고, 선택한 모든 제품을 가진 사례를 기준으로 제품 목록을 계산하도록 했다. `전체`는 선택값을 모두 해제한다.
- 이유: DEV-REV-004와 REQ-010의 다중 AND 필터·전체 해제 규칙을 구현하기 위해서다.
- 관련: DEV-REV-004, REQ-010
- 검증: 콘텐츠 검사·테스트·린트·빌드 재실행 예정.

# 변경 22 — 제품 다중 선택 테스트 추가

- 행동: 수정
- 파일: `tests/cases.test.ts`
- 내용: 여러 제품을 함께 가진 테스트 사례를 사용해 다중 제품 AND 필터 결과를 검증하는 테스트를 추가했다.
- 이유: DEV-REV-004의 필터 동작이 현재 실제 JSON의 단일 제품 사례에만 우연히 의존하지 않도록 하기 위해서다.
- 관련: DEV-REV-004, REQ-010
- 검증: Vitest 재실행 예정.

# 변경 23 — 제품 필터 변경 파일 서식 정리

- 행동: 수정
- 파일: `src/features/products/components/ProductList.tsx`, `src/lib/products.ts`, `src/lib/index.ts`
- 내용: Prettier로 제품 필터 구현 파일의 서식을 프로젝트 설정에 맞췄다.
- 이유: DEV-REV-004 구현 변경의 코드 서식을 통일하기 위해서다.
- 관련: DEV-REV-004
- 검증: 변경 파일 대상 Prettier 실행 완료.

# 변경 24 — 중첩 콘텐츠 랜드마크 제거

- 행동: 수정
- 파일: `src/app/company/page.tsx`, `src/features/cases/components/CaseList.tsx`, `src/features/products/components/ProductList.tsx`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 공통 레이아웃의 `<main>`만 유지하고 세 하위 화면의 중첩 `<main>`을 `<div>`로 변경했다. DEV-REV-006을 해결로 기록했다.
- 이유: 문서에 주 콘텐츠 랜드마크가 하나만 존재하도록 하기 위해서다.
- 관련: DEV-REV-006
- 검증: 이후 린트·테스트·빌드에서 확인 예정.

# 변경 25 — 완료 이슈 상태 정리 및 홈 회사 소개 제거

- 행동: 수정
- 파일: `src/app/page.tsx`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 홈에서 회사 소개 JSON 문구를 표시하던 문단을 제거하고, 검증이 끝난 DEV-REV-004, 006~012, 014에 해결 방법과 검증 결과를 기록했다.
- 이유: 사용자가 홈에는 회사 소개를 넣지 않는다고 확정했으며, 구현·검증이 끝난 이슈를 정확한 상태로 관리하기 위해서다.
- 관련: DEV-REV-004, DEV-REV-006 ~ DEV-REV-012, DEV-REV-014
- 검증: 콘텐츠 검사·Vitest·ESLint·Next.js 빌드 통과.

# 변경 26 — 해결된 이슈의 중앙 목록 제거

- 행동: 수정
- 파일: `harness/review/issue-register.md`
- 내용: 구현·검증이 끝난 DEV-REV-004, DEV-REV-006 ~ DEV-REV-012, DEV-REV-014를 중앙 미해결 목록에서 제거했다.
- 이유: 해결된 이슈는 중앙 목록에서 제거하고 원본 날짜 파일에 해결 방법을 남기는 리뷰 규칙을 적용하기 위해서다.
- 관련: DEV-REV-004, DEV-REV-006 ~ DEV-REV-012, DEV-REV-014
- 검증: 중앙 목록에 현재 미해결 DEV-REV-005와 도메인 대기 DEV-REV-015만 남아 있음을 확인했다.

# 변경 27 — 사례 팝업 키보드 포커스 관리 구현

- 행동: 수정
- 파일: `src/features/cases/components/CaseDetailModal.tsx`
- 내용: 팝업 열림 시 초점을 이동하고, `Tab` 이동을 팝업 안에 제한하며, 사진 뷰어를 닫을 때 이전 요소로 초점을 복귀하도록 했다.
- 이유: DEV-REV-005와 디자인 이관 이슈의 접근성 기준을 구현하기 위해서다.
- 관련: DEV-REV-005, DESIGN-REV-010
- 검증: 린트·테스트·빌드 재실행 예정.

# 변경 28 — 팝업 포커스 구현 파일 서식 정리

- 행동: 수정
- 파일: `src/features/cases/components/CaseDetailModal.tsx`
- 내용: Prettier로 팝업 포커스 구현 파일의 서식을 프로젝트 설정에 맞췄다.
- 이유: DEV-REV-005 구현 변경의 코드 서식을 통일하기 위해서다.
- 관련: DEV-REV-005
- 검증: 변경 파일 대상 Prettier 실행 완료.

# 변경 29 — 페이지 경계 테스트 및 팝업 이슈 해결 기록

- 행동: 수정
- 파일: `tests/cases.test.ts`, `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 0 이하·최대 페이지 초과 입력이 유효 범위로 보정되는 테스트를 추가하고, 포커스 관리 구현이 검증된 DEV-REV-005를 중앙 목록에서 제거했다.
- 이유: DEV-REV-005와 DEV-REV-014의 구현·검증을 완료했기 때문이다.
- 관련: DEV-REV-005, DEV-REV-014
- 검증: 이후 전체 콘텐츠 검사·테스트·린트·빌드를 재실행한다.

# 변경 30 — 제품 필터 이슈 상태 정정

- 행동: 수정
- 파일: `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: 구현과 검증이 완료된 DEV-REV-004의 상태를 해결로 정정하고 해결 방법을 기록했다.
- 이유: 반복되는 상태 문구 패치로 상태 기록이 누락된 것을 확인했기 때문이다.
- 관련: DEV-REV-004
- 검증: 제품 AND 필터 테스트·콘텐츠 검사·린트·빌드 통과.

# 변경 31 — 모달 포커스 수동 검증 대기 상태 반영

- 행동: 수정
- 파일: `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: DEV-REV-005를 중앙 목록에 다시 등록하고 구현 완료·수동 검증 대기 상태로 변경했다.
- 이유: 자동 검증만으로 실제 키보드 포커스 이동을 확인할 수 없어, 접근성 이슈를 완전 해결로 처리하지 않기 위해서다.
- 관련: DEV-REV-005
- 검증: 린트·테스트·빌드 통과, 수동 브라우저 검증 미실행을 명시했다.

### Change 32

- 수정 `src/app/page.tsx`: 히어로 섹션의 전체 화면 `bg-dark`를 제거하고 콘텐츠 컨테이너 내부에만 어두운 배경을 적용했다.
- 수정 `src/app/page.tsx`: 히어로 텍스트 영역의 여백과 이미지 최소 높이를 줄여 과도한 세로 영역을 완화했다.
- 이유: 메인 페이지 양쪽에 불필요한 검정색 배경이 생기고 히어로 영역이 크게 보이는 문제를 수정하기 위해서다.

### Change 33

- 수정 `src/app/page.tsx`: 히어로 텍스트 영역에 좌우 내부 여백을 추가했다.
- 이유: 상단 문구, 제목, 버튼이 히어로 영역의 왼쪽 경계에 붙어 보이는 문제를 해결하기 위해서다.

### Change 34

- 수정 `src/app/layout.tsx`: 메인 콘텐츠를 `main-container` 안에서 렌더링하도록 변경했다.
- 수정 `src/app/globals.css`: 메인 콘텐츠의 최대 너비를 1180px로 제한하고 중앙 정렬·배경·넘침 처리를 추가했다.
- 유지 `src/components/layout/Footer.tsx`: 푸터는 전체 화면 너비 구조를 유지했다.
- 이유: 메인 섹션 배경이 화면 양쪽으로 퍼져 보이는 문제를 줄이고, 메인 콘텐츠가 하나의 중앙 영역 안에서 자연스럽게 정렬되도록 하기 위해서다.

### Change 35

- 수정 `src/app/page.tsx`: 최근 시공 사례 섹션을 흰색 배경으로 변경했다.
- 수정 `src/app/globals.css`: 메인 컨테이너의 기본 배경을 흰색으로 변경했다.
- 이유: 사용 제품 섹션처럼 메인 콘텐츠와 아이템이 흰색 영역 안에 배치되도록 통일하기 위해서다.

### Change 36

- 수정 `src/app/globals.css`: 메인 컨테이너 상단에 24px 여백을 추가했다.
- 이유: 헤더와 메인 콘텐츠가 바로 붙어 보이지 않도록 시각적 간격을 확보하기 위해서다.

### Change 37

- 수정 `src/app/globals.css`: 메인 컨테이너의 상단 마진을 제거하고, 흰색 메인 영역과 첫 번째 `page-container` 사이에 24px 상단 마진을 적용했다.
- 이유: 헤더와 메인 사이가 아니라 메인 흰색 배경과 내부 콘텐츠 영역 사이에 여백을 두려는 요구사항을 반영하기 위해서다.

### Change 38

- 수정 `src/features/cases/components/CaseList.tsx`: 시공 사례 페이지의 첫 영역을 어두운 히어로 영역으로 변경하고 제목·설명 대비를 조정했다.
- 수정 `src/features/products/components/ProductList.tsx`: 사용 제품 페이지의 첫 영역을 어두운 히어로 영역으로 변경하고 제목·설명 대비를 조정했다.
- 유지 `src/app/company/page.tsx`: 회사 소개 페이지의 어두운 히어로 구조를 유지했다.
- 이유: 세부 페이지의 첫 화면이 밋밋하게 보이지 않도록 메인 페이지와 동일한 시각적 위계를 적용하기 위해서다.

### Change 39

- 수정 `src/features/cases/components/CaseList.tsx`, `src/features/products/components/ProductList.tsx`, `src/app/company/page.tsx`: 각 페이지 히어로 영역을 최소 240px 높이의 수직 중앙 정렬 블록으로 통일했다.
- 이유: 페이지별 첫 영역의 높이와 텍스트 위치를 맞춰 메인 페이지와 일관된 화면 비율을 만들기 위해서다.

### Change 40

- 수정 `src/features/cases/components/CaseList.tsx`, `src/features/products/components/ProductList.tsx`, `src/app/company/page.tsx`: 히어로 배경을 전체 `section`에서 내부 `page-container`로 이동했다.
- 이유: 각 페이지 히어로가 메인 페이지처럼 흰색 메인 영역 안에 들어간 독립된 블록으로 보이도록 좌우·상단 여백을 명확하게 만들기 위해서다.

### Change 41

- 수정 `src/features/cases/components/CaseList.tsx`, `src/features/products/components/ProductList.tsx`, `src/app/company/page.tsx`: 바깥 섹션의 중복 패딩을 제거하고, 내부 어두운 `page-container`에 좌우·상하 패딩과 수직 중앙 정렬을 적용했다.
- 이유: 히어로 텍스트가 블록 경계에 붙고 상단 여백이 과하게 생기는 문제를 해결하기 위해서다.

### Change 42

- 수정 `src/app/globals.css`: 첫 번째 메인 섹션에 24px 상단 패딩을 적용했다.
- 이유: CSS 마진 병합으로 사라지던 히어로 상단 여백을 흰색 메인 영역에 확실하게 표시하기 위해서다.

### Change 43

- 수정 `src/app/globals.css`: 루트 섹션과 래퍼 `div` 내부의 첫 섹션 모두에 상단 여백이 적용되도록 선택자를 보완했다.
- 이유: 시공 사례·사용 제품·회사 소개 페이지의 실제 DOM 구조에서도 히어로 위 흰색 여백이 표시되도록 하기 위해서다.

### Change 44

- 수정 `src/app/globals.css`: `html`에 `scrollbar-gutter: stable`을 적용했다.
- 이유: 시공 사례 상세 모달이 열릴 때 `body` 스크롤 잠금으로 스크롤바가 사라져 현재 페이지의 아이템 위치가 이동하는 문제를 방지하기 위해서다.

### Change 45

- 수정 `src/features/cases/components/CaseDetailModal.tsx`: 모달 열기 시 `body` 스크롤 잠금만 적용하고, 별도 오른쪽 여백 보정은 제거했다.
- 수정 `src/features/cases/components/CaseDetailModal.tsx`: 이미지 뷰어 전환 시 스크롤 잠금 효과가 재실행되지 않도록 의존성을 조정했다.
- 이유: 상세 모달과 이미지 뷰어를 여닫을 때 현재 페이지의 아이템 위치가 이동하는 레이아웃 시프트를 방지하기 위해서다.

### Change 46

- 수정 `src/app/globals.css`: `html`에 세로 스크롤바를 항상 유지하도록 설정했다.
- 이유: 모달이 열려도 문서 기준 폭이 유지되어 중앙 컨테이너와 현재 페이지 아이템의 위치가 변하지 않도록 하기 위해서다.

### Change 47

- 수정 `src/features/cases/components/CaseDetailModal.tsx`: 스크롤 잠금 효과와 Escape 키 처리 효과를 분리했다.
- 이유: 이미지 뷰어 전환 때 스크롤 잠금이 재실행되지 않으면서도 최신 모달 상태로 Escape 키를 처리하고, Hook 의존성 경고를 제거하기 위해서다.

### Change 48

- 수정 `src/app/globals.css`: 항상 세로 스크롤바를 표시하던 전역 설정을 제거했다.
- 수정 `src/features/cases/components/CaseDetailModal.tsx`: 모달이 열릴 때만 스크롤바 폭만큼 `body` 오른쪽 여백을 보정하고 닫을 때 복원하도록 변경했다.
- 이유: 평상시 페이지에 불필요한 스크롤바가 보이는 문제를 제거하면서 모달 열림 시 아이템 위치 이동은 방지하기 위해서다.

### Change 49

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 페이지 책임 분리 리뷰 이슈 DEV-REV-016을 등록했다.
- 이유: 웰컴·회사 소개 라우트 파일이 페이지 조립을 넘어 섹션별 화면 구현까지 담당하는 것을 코드 리뷰에서 확인했기 때문이다.

### Change 50

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 사례 목록 기능의 책임 과다 이슈 DEV-REV-017을 등록했다.
- 이유: `CaseList`가 데이터 로드, URL·상태 처리, 목록 화면 조립을 모두 맡는 것을 추가 코드 리뷰에서 확인했기 때문이다.

### Change 51

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 제품 목록의 책임 과다, 사례 상세 모달의 책임 과다, 전화번호 선택값 처리 불일치 이슈를 DEV-REV-018 ~ DEV-REV-020으로 등록했다.
- 이유: `src` 전체를 책임 경계와 콘텐츠 모델 일관성 기준으로 재검토해, 기존 이슈 외에도 수정이 필요한 근거를 확인했기 때문이다.

### Change 52

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 사례 필터·페이지 상태의 sessionStorage 유지 누락을 DEV-REV-021로 등록했다.
- 이유: 승인 기록과 달리 Zustand 저장소가 메모리 상태만 가지는 것을 소스와 결정 문서 대조에서 확인했기 때문이다.

### Change 53

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 사례 장소·자재 태그 변경 규칙의 중복을 DEV-REV-022로 등록했다.
- 이유: 두 이벤트 처리 함수가 같은 선택 교체·페이지 초기화 규칙을 각각 구현하는 것을 사용자 검토에서 확인했기 때문이다.

### Change 54

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 공통 페이지 히어로와 제품 카드 재사용 누락을 DEV-REV-023 ~ DEV-REV-024로 등록하고, DEV-REV-020에 전화번호 주소 생성 중복을 추가했다.
- 이유: `src` 전체의 화면 구조와 데이터 처리 중복을 다시 대조해, 함께 바뀌어야 하는 규칙·구조를 확인했기 때문이다.

### Change 55

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 함수·타입 한 줄 주석 규칙의 미적용을 DEV-REV-025로 등록했다.
- 이유: DEC-067과 코드 스타일 문서의 확정 규칙을 `src`의 실제 TS/TSX 소스와 대조했을 때 역할 주석이 적용되지 않았음을 확인했기 때문이다.

### Change 56

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 홈·사례 목록의 카드와 상세 팝업 선택 구조 중복을 DEV-REV-026으로 등록했다.
- 이유: 두 구현을 직접 대조해 표시 밀도 차이보다 공통 카드 데이터·클릭·상세 열기 구조가 더 크다는 것을 확인했기 때문이다.

### Change 57

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: DEV-REV-026을 중앙 미해결 목록에서 제거하고 철회 사유를 기록했다.
- 이유: 홈·사례 목록 카드의 짧은 공통 부분만으로 공통 컴포넌트를 만들면 표시 옵션과 상태 결합이 오히려 늘어날 수 있음을 사용자 검토에서 확인했기 때문이다.

### Change 58

- 수정 `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: DEV-REV-017과 DEV-REV-022에 사례 목록 상태를 단일 `useCaseListStore`로 통합하는 사용자 확인을 기록했다.
- 이유: 필터·페이지 상태를 한 파일에서 export한 Zustand 스토어로 관리하고, 화면은 그 스토어를 훅으로 구독하는 방향을 사용자가 확정했기 때문이다.

### Change 59

- 수정 `harness/review/issue-register.md`: DEV-REV-017·DEV-REV-022의 중앙 목록 요약에 단일 `useCaseListStore` 통합 결정을 명시했다.
- 이유: 상세 이슈에만 있던 Zustand 상태 통합 방향을 미해결 이슈 목록에서도 바로 확인할 수 있게 하기 위해서다.

### Change 60

- 수정 `harness/review/issue-register.md`, `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: 상태 도구 이름 중심의 `useCaseListStore` 표현을 사례 목록이 기억할 값과 동작이 드러나는 `useCaseList` 방향으로 바꿨다.
- 이유: 상태 관리 도구를 모르는 개발자도 파일의 목적과 사용할 값을 바로 이해할 수 있게 하기 위해서다.

### Change 61

- 수정 `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: DEV-REV-021에 새로고침 후 태그·현재 페이지를 유지하고 탭을 닫으면 초기화한다는 사용자 확인을 기록했다.
- 이유: 사례 목록의 기억 범위를 실제 사용자 동작으로 확정하기 위해서다.

### Change 62

- 수정 `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: DEV-REV-021의 구현 방식을 Zustand persist와 `sessionStorage`, 저장 키 `gk-case-list`로 확정했다.
- 이유: 사례 목록 상태를 새로고침 뒤에도 유지하는 실제 저장 기술과 저장 범위를 사용자 확인으로 명확히 하기 위해서다.

### Change 63

- 수정 `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: DEV-REV-022에 장소 1개·제품 1개 선택과 두 조건의 동시 적용 규칙을 사용자 확인으로 기록했다.
- 이유: 통합된 사례 목록 상태가 태그 선택을 처리할 때 따라야 할 정확한 규칙을 확정하기 위해서다.

### Change 64

- 수정 `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: DEV-REV-024에 모든 제품 카드의 이미지 없음 문구를 `죄송합니다. 이미지는 준비 중입니다.`로 통일하는 기존 확정 기준을 명시했다.
- 이유: 이미 디자인 시스템에서 정한 문구를 홈 카드에도 같은 방식으로 적용해야 함을 사용자 검토에서 재확인했기 때문이다.

### Change 65

- 추가 `src/components/ui/PageHero.tsx`: 사례·제품·회사 소개 화면이 공유하는 첫 영역을 문구 props로 표시하는 공통 컴포넌트를 만들었다.
- 수정 `src/app/cases/page.tsx`, `src/app/products/page.tsx`, `src/app/company/page.tsx`: 각 페이지가 첫 영역 문구를 `PageHero`에 전달하도록 변경했다.
- 수정 `src/features/cases/components/CaseList.tsx`, `src/features/products/components/ProductList.tsx`: 페이지 조립으로 이동한 첫 영역 마크업을 제거했다.
- 이유: DEV-REV-023과 사용자 승인에 따라 같은 첫 영역 구조를 한 파일에서 관리하고, 각 페이지는 문구 전달과 화면 조립을 맡도록 하기 위해서다.
- 검증: lint와 build 실행 예정.

### Change 66

- 삭제 `src/components/ui/PageHero.tsx` 및 관련 코드 변경: 리뷰 단계에서 승인 범위를 넘어 적용한 공통 첫 영역 구현을 되돌렸다.
- 이유: 사용자가 현재 역할을 리뷰어로 한정했으므로, 구현은 이슈 기록과 사용자 승인을 거친 개발 단계에서만 진행해야 한다.

### Change 67

- 수정 `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: DEV-REV-019의 분리 기준을 화면 조각 분리가 아니라 재사용 가능한 기능 분리로 바로잡았다.
- 이유: 화면 조립 파일에 팝업·키보드·스크롤·사진 이동 기능을 남기면 자기책임원칙을 지키지 못하며, 다른 화면에서 같은 기능을 다시 만들어야 한다는 사용자 기준을 반영하기 위해서다.

### Change 68

- Added `docs/design/previews/case-detail-architecture-comparison.html`: architecture deliverable that visually compares the current all-in-one `CaseDetailModal.tsx` with the agreed target structure: `CaseDetailController`, UI-only `CaseDetailModal`, three UI pieces, and reusable dialog/scroll-lock/image-gallery behavior files.
- Why: before implementation, the user requested a concrete HTML artifact that makes the responsibility boundary and the difference from the current detail page easy to review (DEV-REV-019).

### Change 69

- Updated `docs/design/previews/case-detail-architecture-comparison.html`: changed the target diagram to the user's parent-and-child-screen structure. `CaseDetailModal` now only assembles screens 1, 2, and 3; each child screen directly imports only the reusable functionality it needs. Added clear examples of deeper screen splits (1-1, 1-2) and reuse from another screen.
- Why: the previous diagram incorrectly made the connection point between screen and reusable function unclear. This revision documents the requested responsibility boundary in concrete file-level terms.

### Change 70

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: rewrote DEV-REV-019 as an implementable parent-and-child-screen plan. It now defines the responsibilities, direct functionality imports, deeper split examples, and `images` props reuse rule for the dialog, image gallery, and information screens. Added DEV-REV-027 with the same architecture rule as a required review and implementation standard for every TS/TSX file in `src`.
- Updated `harness/review/issue-register.md`: added DEV-REV-027 to the central unresolved list.
- Why: the user confirmed that a reusable image gallery must work for future product detail data through props, and that the responsibility structure must apply across all of `src`, not only the case detail screen.

### Change 71

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md` and `harness/review/issue-register.md`: recorded the user's decision for DEV-REV-020. The GK telephone number is required (`string`), the content check and all screens must use that one rule, and telephone-link conversion remains in each screen without adding a separate shared helper.
- Why: the user confirmed that telephone information is already maintained together in JSON and does not need a separate abstraction; the actual defect is the inconsistent required/optional assumption.

### Change 72

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: added the explicit DEV-REV-016 boundary that the home page contains only its hero, recent cases, and products; company-introduction content remains exclusively on the separate company page.
- Why: the user reaffirmed the existing approved rule from DEV-REV-011 that company introduction must not appear on the home page.

### Change 73

- Added `docs/design/previews/service-architecture-and-flow.html`: a detailed whole-service architecture artifact. It documents the current route flow, global layout, every route's parent and child screen sections, direct feature imports, props, shared screen/function reuse, and the future product-detail reuse of `ImageGallery`.
- Why: the user requested an HTML document that reorganizes every screen in the service using the agreed parent → child screen → direct functionality import structure before development.

### Change 74

- Updated `docs/design/previews/service-architecture-and-flow.html`: added a route-by-route side-by-side comparison of the actual current execution flow and the planned responsibility structure for home, cases, products, and company. Each comparison now shows the exact current parent/file path, data and state flow, rendered sections, and its corresponding target parent and child screens.
- Why: the prior document showed the planned structure in detail but did not make the current structure flow visible enough to compare the necessary refactor work.

### Change 75

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md`, `harness/review/issue-register.md`, and `docs/design/previews/service-architecture-and-flow.html`: corrected the case-list persistence rule. Values persist for a refresh inside `/cases`, but tags and the current page reset when leaving `/cases` for any other route. The home-only `HomePageStateReset` is replaced in the plan by case-list route-exit handling.
- Why: the user corrected the assistant's unsupported assumption that visiting another page should preserve the filters. The approved rule is route-change reset, not tab-close-only reset.

### Change 76

- Updated `AGENTS.md`: added mandatory artifact-completeness rules. Documents must include concrete state lifetime and transition rules, current-versus-target screen structure, parent/child responsibilities, props, direct functionality imports, reuse examples, file actions, and clearly separated unresolved items.
- Why: the user identified repeated loss of confirmed details when later work relied on vague summaries. The new rule requires every document to stand alone for planning, design, development, and review without filling gaps through assumptions.

### Change 77

- Updated `docs/design/previews/service-architecture-and-flow.html`: added a screen-by-screen functionality specification for global UI, home, cases, products, and company. Each entry now states inputs, trigger, state, behavior, reset/persistence timing, direct functionality imports, and reuse rules where applicable.
- Why: the user identified that the whole-service architecture artifact described screen layout but did not yet provide enough functional detail for planning and development.

### Change 78

- Updated `docs/design/previews/service-architecture-and-flow.html`: simplified the artifact to its intended level. It now shows only the overall flow, current-versus-refactored route structure, the functions owned by each screen, and screens/functions separated specifically for reuse.
- Why: the prior revision expanded into implementation-level functional specification. The user requested a lighter architecture artifact focused on where functions live and which parts are reusable.

### Change 79

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md` and `harness/review/issue-register.md`: re-audited unresolved architecture issues against the confirmed parent/child/reuse direction. Replaced obsolete wording, added explicit file moves/removals and shared-component ownership, and recorded the only unconfirmed product-list behavior as `TBD-018-01` rather than assuming it.
- Why: the user requested that every existing issue be checked again and completed where the agreed direction already provides an answer, while separating only decisions that cannot be derived from prior confirmations.

### Change 80

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md`: tied DEV-REV-005 keyboard verification to the agreed CaseDialogScreen architecture and listed the concrete manual checks required after the refactor.
- Why: the re-audit found that the existing manual-verification issue must follow the new responsibility structure instead of referring only to the current all-in-one modal file.

### Change 81

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md` and `harness/review/issue-register.md`: removed the temporary `TBD-018-01` label and recorded the user's decision that selected products remain selected after a `/products` refresh.
- Why: `TBD-018-01` was only an internal marker for an unanswered decision, not a source-code or version number; the user has now answered that question.

### Change 82

- Updated `harness/review/26-08-12-issue/26-08-12-dev-issues.md`, `harness/review/issue-register.md`, and `docs/design/previews/service-architecture-and-flow.html`: applied the user's global filter rule. Case and product selections now persist across every refresh and every page move in the same tab; only each filter screen's explicit reset action clears its values. Product selection is stored as `selectedProductIds` in `sessionStorage` key `gk-product-list`; the existing case key remains `gk-case-list`.
- Why: the user clarified that the persistence rule applies to all filters, not just the product filter, and that page navigation must never reset filter values.

### Change 83

- Added/refactored route screens, reusable UI, shared hooks, and unified case/product list state across `src`. Removed obsolete `CaseList`, `ProductList`, `HomePageStateReset`, and separate case store files.
- Updated `src/types/content.ts` and `scripts/check-content.mjs` so the phone number is required.
- Why: implemented the approved parent/child screen composition, props-based reuse, and persistent filter rules.
- Verification: `npm run lint`, `npm run build`, and `npm run check:content` passed.

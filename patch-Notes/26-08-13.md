## 변경 1 — 소스 코드 역할 주석 보강
- 행동: 수정
- 파일: `src/types/content.ts`
- 내용: 사례 사진·사례·사이트 정보 타입의 데이터 출처와 용도를 한 줄 주석으로 추가했다.
- 이유: 사용자가 요청한 주석 보강 및 `DEV-REV-025`의 타입 주석 규칙 반영.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

## 변경 2 — 주석 규칙 리뷰 이슈 해결 기록
- 행동: 수정
- 파일: `harness/review/issue-register.md`
- 내용: 해결된 `DEV-REV-025`를 미해결 이슈 중앙 목록에서 제거했다.
- 이유: `src` 전체 타입·함수 역할 주석 보강이 완료되어 중앙 목록에는 미해결 이슈만 남겨야 한다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `harness/review/26-08-12-issue/26-08-12-dev-issues.md`
- 내용: `DEV-REV-025`에 해결 방법, 수정 범위, 검증 결과를 기록하고 상태를 해결로 변경했다.
- 이유: 이슈 발생 원본에 해결 방법과 검증 결과를 남기는 프로젝트 규칙을 따른다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

## 변경 1 계속 — 소스 코드 역할 주석 보강
- 행동: 수정
- 파일: `src/types/filter.ts`
- 내용: 사례 목록 계산 결과 타입의 용도를 주석으로 추가했다.
- 이유: 사용자가 요청한 주석 보강 및 `DEV-REV-025`의 타입 주석 규칙 반영.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

## 변경 3 — 사례 상세 팝업 JSX 주석 보강
- 행동: 수정
- 파일: `src/features/cases/components/CaseDetailModal.tsx`
- 내용: 팝업 배경, 배경 닫기 버튼, 상세 팝업, 고정 머리글, 닫기 버튼, 본문 영역의 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 화면을 구성하는 `button`, `section` 등 HTML 요소 단위의 역할 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 9 — 사례 목록 화면 JSX 주석 보강
- 행동: 수정
- 파일: `src/features/cases/components/CasesScreen.tsx`
- 내용: 히어로, 목록 본문, 필터, 결과 요약, 카드 목록, 빈 상태, 페이지네이션, 상세 팝업의 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 화면을 구성하는 HTML 요소 단위의 역할 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 10 — 홈·제품·회사 소개 JSX 주석 보강
- 행동: 수정
- 파일: `src/features/home/components/HomeHero.tsx`, `src/features/home/components/HomeRecentCases.tsx`, `src/features/home/components/HomeProducts.tsx`, `src/features/home/components/HomeCaseCards.tsx`
- 내용: 홈의 대표 사례, 최근 사례, 사용 제품 화면에서 섹션·카드·이미지·이동 링크·상세 팝업의 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 `src`의 모든 화면과 기능에 HTML 요소 단위 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 11 — 공용 UI·레이아웃 JSX 주석 보강
- 행동: 수정
- 파일: `src/components/ui/Container.tsx`, `src/components/ui/PageHero.tsx`, `src/components/ui/ProductCard.tsx`, `src/components/ui/ImageGallery.tsx`
- 내용: 공용 컨테이너·히어로·제품 카드·사진 갤러리의 섹션, 이미지, 버튼, 링크, 뷰어 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 `src`의 모든 화면과 기능에 HTML 요소 단위 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 12 — 주소 페이지 JSX 주석 보강
- 행동: 수정
- 파일: `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/cases/page.tsx`, `src/app/products/page.tsx`, `src/app/company/page.tsx`, `src/features/company/components/CompanyHero.tsx`
- 내용: 최상위 레이아웃, 각 주소의 주요 화면 섹션, 회사 소개 히어로의 역할을 JSX 또는 반환 요소 주석으로 추가했다.
- 이유: 사용자가 `src`의 모든 화면과 기능에 HTML 요소 단위 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/components/layout/Header.tsx`, `src/components/layout/MobileMenu.tsx`, `src/components/layout/Footer.tsx`, `src/components/layout/SocialLinks.tsx`
- 내용: 헤더·모바일 메뉴·푸터·소셜 링크의 메뉴, 버튼, 연락처, 외부 링크 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 `src`의 모든 화면과 기능에 HTML 요소 단위 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/features/products/components/ProductsScreen.tsx`, `src/features/products/components/ProductFilters.tsx`, `src/features/products/components/ProductResults.tsx`
- 내용: 제품 화면의 섹션·필터 버튼·결과 카드·안내 문구 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 `src`의 모든 화면과 기능에 HTML 요소 단위 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/features/company/components/CompanyIntroduction.tsx`, `src/features/company/components/CompanyServiceScopes.tsx`, `src/features/company/components/CompanyContact.tsx`
- 내용: 회사 소개의 문단·카드·전화 문의 섹션과 링크 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 `src`의 모든 화면과 기능에 HTML 요소 단위 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 8 — 사례 페이지네이션 JSX 주석 보강
- 행동: 수정
- 파일: `src/features/cases/components/CasePagination.tsx`
- 내용: 탐색 영역과 이전·번호·다음 버튼의 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 화면을 구성하는 HTML 요소 단위의 역할 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 7 — 최상위 JSX 주석 문법 수정
- 행동: 수정
- 파일: `src/features/cases/components/CaseDetailModal.tsx`, `src/features/cases/components/CaseInformation.tsx`, `src/features/cases/components/CaseCard.tsx`, `src/features/cases/components/CaseFilters.tsx`
- 내용: 최상위 JSX 요소 앞 주석을 반환 표현식 안에서 허용되는 JavaScript 주석 형태로 변경했다.
- 이유: JSX 주석이 최상위 요소 앞에 단독으로 놓여 파싱 오류가 발생한 것을 수정했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 6 — 사례 필터 JSX 주석 보강
- 행동: 수정
- 파일: `src/features/cases/components/CaseFilters.tsx`
- 내용: 필터 머리글, 초기화 버튼, 태그 그룹, 스크롤 목록, 태그 선택 버튼의 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 화면을 구성하는 HTML 요소 단위의 역할 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 5 — 사례 카드 JSX 주석 보강
- 행동: 수정
- 파일: `src/features/cases/components/CaseCard.tsx`
- 내용: 카드 버튼, 대표 사진·대체 안내, 카드 정보 영역의 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 화면을 구성하는 HTML 요소 단위의 역할 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

## 변경 4 — 사례 상세 정보 JSX 주석 보강
- 행동: 수정
- 파일: `src/features/cases/components/CaseInformation.tsx`
- 내용: 상세 정보 영역, 시공 내용, 정의 목록, 장소·제품·태그 정보 행의 역할을 JSX 주석으로 추가했다.
- 이유: 사용자가 화면을 구성하는 HTML 요소 단위의 역할 주석을 요청했다.
- 관련: 없음
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/lib/cases.ts`
- 내용: 태그 추출, 사례 필터·페이지 계산, 페이지 번호 계산 함수의 역할 주석을 추가했다.
- 이유: 사용자가 함수의 입력과 책임을 빠르게 파악할 수 있도록 요청했다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/lib/products.ts`
- 내용: 제품명·대표 사례·표시 대상 제품 계산 함수의 역할 주석을 추가했다.
- 이유: 사용자가 함수의 입력과 책임을 빠르게 파악할 수 있도록 요청했다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/lib/home-content.ts`
- 내용: 홈 대표 사례·제품 데이터와 대표 사례 조회 함수의 역할 주석을 추가했다.
- 이유: 정적 JSON에서 만든 홈 전용 데이터를 명확히 구분하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/lib/constants.ts`, `src/lib/navigation.ts`
- 내용: 공용 목록 상수와 공용 메뉴 데이터의 용도 주석을 추가했다.
- 이유: 값이 여러 화면에서 공유되는 이유를 코드 가까이에 남기기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/components/ui/Container.tsx`, `src/components/ui/PageHero.tsx`, `src/components/ui/ProductCard.tsx`, `src/components/ui/ImageGallery.tsx`
- 내용: 공용 UI props 타입의 데이터 의미를 주석으로 추가했다.
- 이유: 공용 화면을 사용할 때 필요한 입력값을 주니어 개발자가 빠르게 파악하도록 하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/features/cases/state/useCaseList.ts`, `src/features/products/state/useProductList.ts`
- 내용: 탭 단위로 보존하는 목록 상태 타입의 범위를 주석으로 추가했다.
- 이유: 저장되는 화면 상태와 변경 함수를 명확히 하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/features/cases/components/CasesScreen.tsx`, `src/features/cases/components/CaseFilters.tsx`, `src/features/cases/components/CasePagination.tsx`, `src/features/cases/components/CaseCard.tsx`, `src/features/cases/components/CaseDetailModal.tsx`, `src/features/cases/components/CaseInformation.tsx`
- 내용: 사례 화면 props 타입과 태그 묶음 함수의 역할 주석을 추가했다.
- 이유: 사례 화면의 데이터 흐름과 각 화면 요소의 책임을 코드에서 바로 확인하도록 하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/features/products/components/ProductsScreen.tsx`, `src/features/products/components/ProductFilters.tsx`, `src/features/products/components/ProductResults.tsx`
- 내용: 제품 화면 props 타입의 데이터 의미를 주석으로 추가했다.
- 이유: 제품 화면이 사례 JSON을 어떻게 사용하는지 명확히 하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/features/home/components/HomeHero.tsx`, `src/features/home/components/HomeRecentCases.tsx`, `src/features/home/components/HomeProducts.tsx`, `src/features/home/components/HomeCaseCards.tsx`
- 내용: 홈 화면 props 타입과 사례 카드 함수의 역할 주석을 추가했다.
- 이유: 홈 각 섹션에 전달되는 데이터와 선택 동작을 구분하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/features/company/components/CompanyHero.tsx`, `src/features/company/components/CompanyIntroduction.tsx`, `src/features/company/components/CompanyServiceScopes.tsx`, `src/features/company/components/CompanyContact.tsx`
- 내용: 회사 소개 화면 props 타입의 데이터 의미를 주석으로 추가했다.
- 이유: site.json의 각 정보가 어느 화면으로 전달되는지 명확히 하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/cases/page.tsx`, `src/app/products/page.tsx`
- 내용: 공통 레이아웃과 각 주소 부모 함수의 데이터 준비·화면 조립 역할 주석을 추가했다.
- 이유: 주소 파일과 기능 화면의 책임 경계를 명확히 하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

- 행동: 수정
- 파일: `src/components/layout/Header.tsx`, `src/components/layout/MobileMenu.tsx`, `src/components/layout/Footer.tsx`, `src/components/layout/SocialLinks.tsx`
- 내용: 공통 레이아웃 함수의 표시 책임을 주석으로 추가했다.
- 이유: 모든 페이지에서 재사용하는 화면 요소의 역할을 빠르게 파악하도록 하기 위해서다.
- 관련: `DEV-REV-025`
- 검증: `npm run lint` 통과.

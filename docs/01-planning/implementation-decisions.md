# 구현 계획 결정 기록

## DEC-011 — 프런트엔드와 배포 방식

- 결정: Next.js와 TypeScript로 구현하고, 서버·DB 없이 정적 결과물을 배포한다.
- 이유: 이 서비스는 전화 문의 중심의 소개 사이트이며, 정적 JSON 데이터와 페이지별 검색 노출이 필요하다.
- 영향: REST API, 데이터베이스, 인증, 업로드 기능은 도입하지 않는다. 콘텐츠 변경 후에는 다시 빌드·배포한다.
- 상태: 확정
- 최종 확인: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 이 기술 방향을 다시 승인했다.

## DEC-012 — 이미지 파일과 경로 관리

- 결정: 실제 사진은 `public/images/` 아래에 저장하고, 사례·제품 JSON에는 `/images/...` 형식의 경로 문자열만 저장한다.
- 예시: `public/images/cases/case-001/01.jpg` → JSON의 `"/images/cases/case-001/01.jpg"`
- 이유: 사진 교체와 사례·제품 데이터 수정을 분리해 관리하고, 정적 배포 환경에서 단순하게 제공하기 위함이다.
- 상태: 확정
- 최종 확인: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 이 사진 관리 방식을 다시 승인했다.

## DEC-013 — 콘텐츠 JSON 폴더

- 결정: JSON 파일은 `data/` 대신 프로젝트 루트의 `content/` 폴더에서 용도별로 관리한다.
- 구성: `cases.json`, `products.json`, `site.json`, `seo.json`
- 이유: 이 파일들은 개발용 데이터가 아니라 사이트에 표시할 콘텐츠이므로, 비개발자도 의미를 이해하기 쉬운 이름을 사용한다.
- 상태: 확정
- 최종 확인: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 이 JSON 콘텐츠 관리 방식을 다시 승인했다.

## DEC-014 — 시공 사례 상세 주소

- 결정: 시공 사례 상세 주소는 고유 번호와 읽기 쉬운 이름을 결합한다.
- 형식: `/cases/case-001-hwaseong-factory-epoxy`
- JSON: 내부 식별자 `id`와 주소용 `slug`를 함께 관리한다.
- 이유: 사례명이나 공법이 같아져도 고유 번호로 주소 중복을 막고, 주소만 보아도 사례 내용을 짐작할 수 있게 하기 위함이다.
- 상태: 확정

## DEC-015 — 시공 사례 대표 이미지

- 결정: 시공 사례 JSON에 목록·웰컴 페이지용 대표 사진의 위치를 `thumbnailIndex` 숫자로 둔다.
- 형식: `"thumbnailIndex": 0` → 화면에서는 `images[thumbnailIndex]`를 사용한다.
- 규칙: 이미지 경로와 대체 텍스트는 `images` 배열에서 한 번만 관리한다.
- 이유: 대표 이미지 경로를 중복 저장하지 않고, 목록·웰컴 페이지와 상세 사진이 같은 이미지 정보를 재사용하게 하기 위함이다.
- 상태: 확정

## DEC-016 — 화면 주소 구조

- 결정: Next.js 라우팅 주소를 화면 역할별로 단순하게 관리한다.
- 주소:
  - `/` — 웰컴 페이지
  - `/cases` — 시공 사례 목록
  - `/cases/case-001-hwaseong-factory-epoxy` — 시공 사례 상세
  - `/products` — 사용 제품 목록
  - `/products/epoxy` — 사용 제품 상세
  - `/company` — 회사 소개
- 이유: 사용자가 React/Next 라우팅 주소의 의미를 확인했고, 제품·회사 소개를 읽기 쉬운 단순 주소로 관리하기로 했다.
- 상태: 확정

## DEC-017 — 목록 상태 관리

- 결정: Zustand를 사용해 시공 사례와 사용 제품 목록의 필터·페이지 번호 상태를 관리한다.
- 저장: Zustand `persist` 설정으로 `sessionStorage`에 탭별 저장한다.
- 저장 예시: `{"selectedTags":["공장","에폭시"],"page":2}`
- 범위: 목록 필터와 페이지 번호에만 사용한다. URL 쿼리와 서버·엔드포인트 상태 연동은 초기 범위에서 제외한다.
- 이유: 사용자가 Zustand 사용 경험을 쌓고 싶어 하며, 주소를 복잡하게 만들지 않고 목록 상태를 유지하기 위함이다.
- 상태: 확정
- 최종 확인: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 Zustand·sessionStorage 사용과 URL 쿼리 미사용을 다시 승인했다.

## DEC-018 — 패키지 관리자와 의존성 기록

- 결정: 패키지 설치와 실행은 `npm`으로 통일한다. 설치한 패키지와 사용 목적은 `docs/03-development/dependencies.md`에 기록한다.
- 기록 항목: 패키지명, 버전, 설치 이유, 사용하는 위치, 제거 가능 여부.
- 이유: 주니어 개발자도 프로젝트에 사용된 도구와 이유를 쉽게 파악하고, 불필요한 의존성을 관리할 수 있게 하기 위함이다.
- 상태: 확정

## DEC-019 — 코드 검사와 자동 서식

- 결정: TypeScript와 함께 ESLint를 사용해 코드 실수를 검사하고, Prettier를 사용해 코드 서식을 통일한다.
- 설정 기록: 팀 공통 ESLint·Prettier 설정과 줄 바꿈 규칙은 `docs/03-development/code-style.md`에 기록한다.
- 이유: TypeScript가 찾지 못하는 React·Next.js 작성 실수를 확인하고, 개발자별 서식 차이로 불필요한 Git 변경이 생기는 것을 줄이기 위함이다.
- 상태: 확정

## DEC-020 — 줄 바꿈 방식

- 결정: 프로젝트의 줄 바꿈 방식은 LF로 통일한다.
- 적용: 개발 단계에서 Prettier의 `endOfLine` 및 Git 관련 설정을 LF 기준으로 맞춘다.
- 이유: 운영체제가 달라 줄 바꿈만 변경된 것처럼 보이는 Git 변경을 줄이기 위함이다.
- 상태: 확정

## DEC-021 — 들여쓰기

- 결정: 들여쓰기는 탭 문자 대신 공백 2칸으로 통일한다.
- 적용: 개발 단계에서 Prettier의 `tabWidth`를 `2`, `useTabs`를 `false`로 설정한다.
- 이유: VS Code를 포함한 편집기 설정이 달라도 저장되는 코드 모양을 일정하게 유지하기 위함이다.
- 상태: 확정

## DEC-022 — 한 줄 최대 길이

- 결정: 코드 한 줄의 최대 길이는 100자로 통일한다.
- 적용: 개발 단계에서 Prettier의 `printWidth`를 `100`으로 설정한다.
- 이유: 긴 코드를 읽기 쉽게 나누되, 불필요하게 많은 줄 바꿈이 생기는 것을 피하기 위함이다.
- 상태: 확정

## DEC-023 — 세미콜론 사용

- 결정: JavaScript·TypeScript 코드 문장 끝에는 세미콜론을 항상 사용한다.
- 적용: 개발 단계에서 Prettier의 `semi`를 `true`로 설정한다.
- 이유: 문장 경계를 명확히 하고, 자동 서식 결과를 예측 가능하게 유지하기 위함이다.
- 상태: 확정

## DEC-024 — 문자열 따옴표

- 결정: JavaScript·TypeScript 문자열은 큰따옴표(`"`)로 통일한다.
- 적용: 개발 단계에서 Prettier의 `singleQuote`를 `false`로 설정한다.
- 이유: 사용자가 Java에서 사용하던 문자·문장 구분 방식에 맞춰 스크립트 문자열을 큰따옴표로 통일하기로 했다.
- 상태: 확정

## DEC-025 — 마지막 쉼표

- 결정: 여러 줄로 작성한 배열·객체·함수 인자의 마지막 항목 뒤에는 쉼표를 항상 사용한다.
- 적용: 개발 단계에서 Prettier의 `trailingComma`를 `all`로 설정한다.
- 이유: 항목을 추가하거나 삭제할 때 필요한 Git 변경 범위를 작게 유지하기 위함이다.
- 상태: 확정

## DEC-026 — 개발 폴더 구조와 디자인 시스템 위치

- 결정: Next.js의 `app/` 폴더를 주소 구조로 사용하고, 화면 조각은 `components/`, 디자인 값은 `styles/tokens.css`, 디자인 기준 문서는 `docs/02-design/design-system.md`에서 관리한다.
- 이유: 페이지·재사용 화면·정적 콘텐츠·상태·디자인 기준의 책임을 분리해 주니어 개발자도 쉽게 찾고 수정할 수 있게 하기 위함이다.
- 상태: 확정
- 최종 확인: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 기존 확정 구조의 사용을 다시 승인했다.

## DEC-027 — 커밋 전 코드 검사

- 결정: Git 커밋 직전에 변경한 코드만 ESLint 검사와 Prettier 서식 검사를 자동 실행한다.
- 도구: 개발 단계에서 Husky와 lint-staged를 사용한다.
- 이유: 공통 코드 규칙과 다른 파일이 GitHub에 올라가는 것을 막고, 전체 프로젝트를 매번 검사하지 않아 커밋을 빠르게 유지하기 위함이다.
- 상태: 확정

## DEC-028 — 자동 테스트와 실패 대응

- 결정: 태그 필터·페이지 계산처럼 결과를 명확히 확인할 수 있는 핵심 데이터 규칙에 자동 테스트를 사용한다.
- 실패 대응: 테스트에서 문제가 발견되면 개발자는 문제 내용, 영향, 예정 조치를 사용자에게 보고하고 승인받기 전에는 수정하지 않는다.
- 금지: 테스트 실패를 해결하기 위한 코드 작성·테스트 실행의 반복을 사용자 승인 없이 진행하지 않는다.
- 이유: 자동 테스트는 품질 확인에 사용하되, 요구사항이나 수정 범위의 판단은 사용자가 통제하도록 하기 위함이다.
- 상태: 확정

## DEC-029 — 자동 테스트 도구

- 결정: 핵심 데이터 규칙의 자동 테스트 도구로 Vitest를 사용한다.
- 적용: 개발 단계에서 Vitest를 설치하고, 첫 필터 규칙 테스트를 작성해 사용자가 실제 실행 결과를 확인할 수 있게 한다.
- 변경: 사용자가 필요 없거나 맞지 않다고 판단하면 Vitest와 관련 설정·테스트 파일을 별도 승인 후 제거할 수 있다.
- 이유: 사용자가 우선 실제로 사용해 본 뒤 유지 여부를 판단하기로 했다.
- 상태: 확정

## DEC-030 — 사진 표시 방식

- 결정: 시공·제품 사진은 Next.js의 `Image` 기능으로 표시한다.
- 적용: 정적 배포 환경에 맞춰 서버 이미지 변환은 사용하지 않고, `public/images/`의 사진 경로를 그대로 사용한다. 모든 사진에는 의미 있는 대체 문구를 둔다.
- 기대 효과: 사진 크기를 미리 잡아 화면 흔들림을 줄이고, 화면 아래 사진은 필요한 시점에 불러와 초기 로딩을 가볍게 한다.
- 이유: 사용자가 Next.js Image 기능 사용을 승인했다.
- 상태: 확정

## DEC-031 — JSON 기반 정적 상세 페이지 생성

- 결정: 시공 사례와 사용 제품 상세 페이지는 빌드할 때 `content/`의 JSON 데이터를 읽어 미리 생성한다.
- 적용: 사례 JSON의 `slug`로 `/cases/[slug]` 주소를 만들고, 제품 JSON의 `slug`로 `/products/[slug]` 주소를 만든다.
- 운영: JSON에 사례·제품을 추가하거나 수정한 뒤에는 다시 빌드·배포해야 변경 사항이 사이트에 반영된다.
- 이유: 사용자가 서버·API 요청 없이 JSON 데이터로 상세 주소를 자동 생성하는 방식을 사용하기로 했다.
- 상태: 확정

## DEC-032 — JSON 콘텐츠 사전 검사

- 결정: 빌드 전에 정적 JSON 콘텐츠의 필수 정보와 연결 관계를 검사한다.
- 검사 범위: 사례·제품 `slug` 중복, `thumbnailIndex` 범위, 필수 제목·태그·사진 경로, 사례가 참조한 제품 ID의 존재 여부.
- 실패 대응: 문제가 발견되면 자동 수정하지 않고, 문제 내용·영향·예정 조치를 사용자에게 먼저 보고하고 승인받은 뒤 수정한다.
- 이유: 잘못된 콘텐츠가 배포용 페이지에 반영되는 것을 막되, 수정 판단은 사용자가 통제하도록 하기 위함이다.
- 상태: 확정

## DEC-033 — 없는 주소 안내 화면

- 결정: 존재하지 않는 주소나 없는 시공 사례·제품 상세 주소에는 404 안내 화면을 표시한다.
- 적용: Next.js의 `app/not-found.tsx`를 사용하고, 안내 문구와 시공 사례 목록으로 돌아가는 동선을 제공한다.
- 이유: 방문자가 빈 화면이나 기술 오류 대신 다음 행동을 선택할 수 있게 하기 위함이다.
- 상태: 확정

## DEC-034 — 오류와 주소 이동 대응

- 결정: 404뿐 아니라 화면 실행 중 오류, 콘텐츠 데이터 오류, 향후 주소 변경에 대비한 오류·이동 정책을 둔다.
- 화면 오류: Next.js의 `app/error.tsx`와 `app/global-error.tsx`로 일반 안내 화면과 홈·시공 사례 목록 이동 동선을 제공한다.
- 콘텐츠 오류: 빌드 전 JSON 검사를 통과하지 못하면 배포용 파일 생성을 중단하고 사용자에게 보고한다.
- 300번대 주소 이동: 초기에는 별도 이동 주소를 만들지 않는다. 도메인·주소 변경이 필요해지면 기존 주소, 새 주소, 이동 이유를 기록하고 301 또는 302를 사용자 승인 후 설정한다.
- 500번대 응답: 정적 배포 사이트는 일반 서버 처리 코드가 없어 애플리케이션이 500 응답을 만들지 않는다. 호스팅 서비스 장애 등 외부 5xx는 호스팅 서비스의 안내 화면 영역이며, 사이트 내부 화면 실행 오류는 위 오류 화면으로 대응한다.
- 이유: 사용자가 404 외의 오류와 주소 이동 가능성까지 대비하도록 요청했다.
- 상태: 확정

## DEC-035 — 정적 배포 서비스

- 결정: Cloudflare Pages와 GitHub 연동으로 정적 사이트를 배포한다.
- 배포 흐름: PR 브랜치는 검토용 미리보기 주소로 배포하고, `main` 브랜치에 머지된 변경은 실제 사이트로 자동 배포한다.
- 빌드 기준: Next.js 정적 내보내기 설정을 사용하며, 빌드 명령은 `npx next build`, 배포 폴더는 `out`으로 계획한다.
- 범위: 실제 Cloudflare 계정 연결, Pages 프로젝트 생성, 도메인·DNS 연결은 배포 단계에서 사용자 승인 후 진행한다.
- 이유: 사용자가 익숙한 Cloudflare를 사용하고, 현재 서버 없는 정적 소개 사이트의 상업적 운영과 GitHub 미리보기·자동 배포에 적합하기 때문이다.
- 상태: 확정

## DEC-036 — PR 자동 검사

- 결정: PR에 변경을 올릴 때 GitHub Actions로 ESLint, Vitest 자동 테스트, 정적 빌드 검사를 실행한다.
- 실패 대응: 검사 실패 시 개발자는 실패 내용·영향·예정 조치를 사용자에게 먼저 보고하고 승인 전에는 수정하지 않는다.
- 구분: 이 검사는 품질 확인용이며, Cloudflare Pages의 미리보기 배포와 별도로 GitHub PR에서 결과를 표시한다.
- 이유: 사용자가 PR 단계의 자동 검사를 승인했고, 병합 전 오류를 확인하기 위해서다.
- 상태: 확정

## DEC-037 — 방문 통계

- 결정: Cloudflare Web Analytics를 사용해 방문·페이지 조회·유입 경로·기기 유형·페이지 로딩 성능을 확인한다.
- 적용: Cloudflare Pages 프로젝트의 Metrics에서 Web Analytics를 활성화한다. Pages는 다음 배포 때 필요한 스크립트를 자동으로 넣는다.
- 개인정보: Cloudflare는 이 기능이 방문자의 개인 데이터를 수집·사용하지 않는다고 안내한다. 초기 범위에는 사용자 개인 식별, 맞춤 이벤트, UTM 쿼리 분석을 넣지 않는다.
- 운영: 통계 데이터는 Cloudflare 대시보드에서 확인한다. 현재 조회 가능 기간은 최근 6개월이다.
- 이유: 사용자가 방문 통계 기능을 추가해 사이트 방문과 인기 페이지·성능을 확인하고자 했다.
- 상태: 확정

## DEC-038 — 콘텐츠 변경 배포 흐름

- 결정: 시공 사례·사용 제품·사이트 문구·사진 등 `content/` JSON과 이미지 변경도 코드 변경과 같은 PR 검토 흐름으로 관리한다.
- 흐름: 작업 브랜치 생성 → JSON·사진 변경 → PR 생성 → Cloudflare Pages 미리보기 확인 → `main` 머지 → 실제 사이트 자동 배포.
- 이유: 콘텐츠 변경이 실제 사이트에 반영되기 전에 미리보기에서 사진·문구·상세 주소를 확인할 수 있게 하기 위함이다.
- 상태: 확정

## DEC-039 — 브라우저 지원 범위

- 결정: Chrome·Edge·Firefox는 최근 3년 안의 버전, Safari·모바일 Safari는 최근 2년 안의 버전을 지원 대상으로 둔다. Internet Explorer는 지원 대상에서 제외한다.
- 적용: 불필요한 최신 문법·브라우저 전용 기능은 피하고, 지원 범위에서는 목록·상세·사진·필터·전화 문의가 동일하게 동작하는지 확인한다.
- 보완: 더 오래된 브라우저에서는 화면 모양이 일부 달라질 수 있으나, 기본 정보와 전화 문의는 가능한 범위에서 표시한다.
- 이유: 사용자가 너무 오래되지 않은 주요 브라우저를 폭넓게 지원하되, 과도한 호환성 코드로 복잡해지는 것은 피하기로 했다.
- 상태: 확정

## DEC-041 — 미리보기 검색 노출 차단

- 결정: Cloudflare Pages의 PR·브랜치 미리보기 배포는 검색엔진에 노출되지 않도록 유지한다.
- 적용: Cloudflare Pages 미리보기 배포에는 기본으로 `X-Robots-Tag: noindex` 응답 헤더가 적용된다. 배포 단계에서 실제 미리보기 주소의 응답 헤더를 확인한다.
- 범위: 실제 사이트인 `main` 배포는 검색 노출 대상이며, 미리보기만 차단한다.
- 이유: 사용자가 작업 중인 미리보기 주소가 Google·네이버 검색 결과에 섞이지 않도록 요청했다.
- 상태: 확정

## DEC-042 — npm 잠금 파일 관리

- 결정: npm이 만드는 `package-lock.json` 파일은 항상 Git에 포함한다.
- 이유: 개발자 컴퓨터, GitHub Actions, Cloudflare Pages가 같은 정확한 패키지 버전을 설치하도록 하기 위함이다.
- 상태: 확정

## DEC-043 — 외부 오류 추적 서비스

- 결정: 초기 범위에는 Sentry 등 외부 오류 추적 서비스를 도입하지 않는다.
- 대응: 사이트 오류 안내 화면, PR 자동 검사, Cloudflare Pages 빌드 로그를 우선 사용한다.
- 변경: 실제 운영 중 오류 원인 추적이 필요해지면 도입 목적·수집 범위·비용을 별도로 검토하고 사용자 승인 후 추가한다.
- 이유: 서버 기능 없는 초기 소개 사이트의 의존성과 외부 서비스 구성을 단순하게 유지하기 위함이다.
- 상태: 확정

## DEC-044 — 디자인 시스템 기본 색상

- 결정: 승인된 시안의 밝은 회색 배경, 흰색 카드, 진한 회색 글자, 주황 강조색 조합을 GK 산업의 기본 색상으로 사용한다.
- 강조색: `#E36F3F`는 전화 문의, 선택 필터, 태그에 제한적으로 사용한다.
- 이유: 시공 사진을 중심으로 두면서 전화 문의와 선택 상태는 명확히 안내하기 위함이다.
- 상태: 확정

## DEC-045 — 디자인 시스템 글자와 간격

- 결정: Pretendard 기반 글꼴, 본문 400·강조 600·큰 제목 700의 굵기 체계, 4·8px 기반 간격 체계를 사용한다.
- 적용: 대표 제목·페이지 제목·구역 제목·카드 제목·본문·보조 문구·태그의 PC·모바일 크기는 디자인 시스템 문서의 표를 따른다.
- 이유: 사용자가 임의의 크기를 고르는 대신 현업에서 널리 쓰는 단순한 글자·간격 기준을 그대로 사용하기로 했다.
- 상태: 확정

## DEC-046 — 디자인 시스템 공통 요소

- 결정: 버튼·필터·카드·페이지 번호는 직선 테두리 중심의 공통 요소 규칙을 사용한다.
- 적용: 전화 문의는 주황 버튼, 주요 이동은 진한 버튼, 일반 이동은 흰 버튼, 선택된 필터는 연한 주황 배경으로 표시한다.
- 이유: 사용자가 승인한 산업 현장형 시안의 사진 중심 분위기를 유지하면서 주요 행동과 선택 상태를 명확히 보여주기 위함이다.
- 상태: 확정

## DEC-047 — 디자인 시스템 사진 표시

- 결정: 목록 카드 사진은 4:3, 웰컴 대표 사진은 16:10, 상세 갤러리 사진은 원본 비율 유지 방식으로 표시한다.
- 적용: 상세 갤러리에는 사진 이동 버튼·현재 위치·썸네일을 항상 표시하고, 모든 사진에는 대체 문구를 둔다.
- 이유: 사용자가 목록에서는 사진을 비교하기 쉽게, 상세에서는 사진이 잘리지 않게 보는 기준을 승인했다.
- 상태: 확정

## DEC-048 — 디자인 시스템 반응형 기준

- 결정: 모바일(767px 이하)·태블릿(768px~1023px)·데스크톱(1024px 이상) 구간으로 화면을 구성하고, 모든 화면에서 같은 기능을 제공한다.
- 적용: 모바일은 햄버거 메뉴·16px 좌우 여백·2열 카드·가로 이동 필터를 사용하고, 데스크톱은 최대 내용 폭 1,180px과 헤더 아래 고정 메뉴를 사용한다.
- 이유: 사용자가 승인한 PC·모바일 화면의 기능 동등성과 사진 중심 목록 구성을 실무형 반응형 기준으로 통일하기 위함이다.
- 상태: 확정

## DEC-040 — 사진 추가와 용량 관리

- 결정: 초기에는 사진 자동 변환·압축 도구를 사용하지 않는다. 관리자는 웹용 사진을 `public/images/`에 바로 추가하고 JSON에 경로만 적는다.
- 성능 대응: Next.js `Image`의 필요한 시점 불러오기를 사용한다. 특정 사진이 지나치게 커서 화면 속도에 문제가 생길 때만 개발자가 해당 파일의 영향과 축소 조치를 먼저 보고하고 승인 후 처리한다.
- 원본: 원본 사진 보관은 저장소 밖에서 별도로 관리하며, 사이트 저장소에는 표시용 사진만 둔다.
- 이유: 사용자가 사진 하나를 추가할 때마다 별도 변환 명령을 실행하는 과정을 피하고자 했다.
- 상태: 확정

## DEC-049 — 개발 도구와 코드 서식 최종 확인

- 결정: `npm`, ESLint, Prettier를 사용한다. 줄 바꿈은 LF, 들여쓰기는 공백 2칸, 한 줄 최대 길이는 100자, 세미콜론은 항상 사용, 문자열은 큰따옴표, 여러 줄 마지막 쉼표는 항상 사용한다.
- 범위: DEC-018~DEC-025의 기존 결정을 하나의 실제 개발 기준으로 다시 확인한다.
- 이유: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 코드 품질·서식 묶음을 최종 승인했다.
- 상태: 확정

## DEC-050 — 자동 검사와 수정 승인 절차 최종 확인

- 결정: 핵심 데이터 규칙에는 Vitest 자동 테스트를 사용하고, PR에서는 ESLint·Vitest·정적 빌드 검사를 실행한다. 검사 실패가 발견되면 개발자는 문제 내용, 영향, 예정 조치를 먼저 사용자에게 보고하고 승인 전에는 수정하지 않는다.
- 금지: 실패 해결을 위해 코드 수정과 검사 실행을 사용자 승인 없이 반복하지 않는다.
- 범위: DEC-028, DEC-029, DEC-032, DEC-036의 기존 결정을 실제 개발 절차로 다시 확인한다.
- 이유: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 자동 검사 사용과 수정 승인 절차를 최종 승인했다.
- 상태: 확정

## DEC-051 — 배포 흐름과 미리보기 검색 차단 최종 확인

- 결정: Cloudflare Pages와 GitHub을 연결한다. PR·브랜치 변경은 검색 노출을 막은 미리보기 주소로 배포하고, `main` 머지 후 실제 사이트로 자동 배포한다.
- 범위: Cloudflare 계정 연결, Pages 프로젝트 생성, 도메인·DNS 연결은 배포 단계에서 사용자의 별도 승인을 받은 뒤 진행한다.
- 이유: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 이 배포 흐름을 최종 승인했다.
- 관련: DEC-035, DEC-041
- 상태: 확정

## DEC-052 — 검색 노출 기본 구조 최종 확인

- 결정: 개발 범위에 페이지별 제목·설명, 의미 있는 주소, 사진 대체 문구, `sitemap`, `robots`, 업체 구조화 정보를 포함한다.
- 기대 효과: Google·네이버가 GK 산업의 바닥 시공 사례·사용 제품 페이지를 이해하고 검색 결과에 노출할 수 있는 기본 구조를 제공한다.
- 제한: 개발만으로 특정 검색어의 상위 노출이나 등록을 보장하지 않는다. 실제 검색 등록은 배포 후 사용자의 별도 승인 작업이다.
- 이유: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 관련 검색어 노출을 위한 기본 구조의 구현을 승인했다.
- 상태: 확정

## DEC-053 — 방문 통계 최종 확인

- 결정: Cloudflare Web Analytics로 방문 수, 페이지 조회, 유입 경로, 기기 유형, 페이지 로딩 성능을 확인한다.
- 제외: 개인 식별, 맞춤 이벤트, 회원 정보 수집, UTM 분석은 초기 범위에 넣지 않는다.
- 이유: 2026-08-08 구현 계획 검토 인터뷰에서 사용자가 개인 추적 없이 기본 방문 통계를 확인하는 기능을 승인했다.
- 관련: DEC-037
- 상태: 확정

# 2026-08-11 변경 기록

## 프로젝트 폴더 구조 재분류

- 행동: 이동
- 파일: `app/**` → `src/app/**`
- 내용: Next.js 라우트와 공통 스타일 파일을 제품 소스 영역으로 이동했다.
- 이유: 사용자의 폴더 구조 정리 요청에 따라 실제 서비스 코드와 프로젝트 운영 산출물을 구분하기 위해서다.

- 행동: 이동
- 파일: `components/layout/**`, `components/ui/**` → `src/components/**`
- 내용: 여러 기능에서 공유하는 레이아웃과 UI 컴포넌트를 공통 제품 소스 영역으로 이동했다.
- 이유: 공통 컴포넌트와 기능 전용 컴포넌트의 책임을 구분하기 위해서다.

- 행동: 이동
- 파일: `components/cases/**`, `components/home/**`, `components/products/**` → `src/features/*/components/**`
- 내용: 사례·홈·제품 화면 컴포넌트를 기능별 폴더로 이동했다.
- 이유: React만 학습한 주니어 개발자도 화면 기능별 진입점을 찾기 쉽게 하기 위해서다.

- 행동: 이동
- 파일: `content/**`, `lib/**`, `types/**` → `src/content/**`, `src/lib/**`, `src/types/**`
- 내용: 정적 콘텐츠, 공통 처리 함수, 공통 타입을 제품 소스 영역으로 이동했다.
- 이유: 배포되는 코드와 데이터를 `src/` 아래에서 일관되게 찾을 수 있도록 하기 위해서다.

- 행동: 이동
- 파일: `store/**` → `src/features/cases/store/**`
- 내용: 기존 상태 파일을 실제 사용 기능인 사례 영역으로 이동했다.
- 이유: 경로 변경 중 기능을 보존하면서 상태의 소유 범위를 명확히 하기 위해서다.

- 행동: 이동
- 파일: `roles/**`, `patchNote/**`, `docs/04-review/**` → `harness/roles/**`, `harness/patch-notes/**`, `harness/review/**`
- 내용: AI 역할, 변경 기록, 리뷰 기록을 하네스 영역으로 이동했다.
- 이유: AI 운영 파일과 제품 코드·승인 산출물을 구분하기 위해서다.

- 행동: 이동
- 파일: `docs/01-planning/**`, `docs/02-design/**`, `docs/03-development/**` → `docs/requirements/**`, `docs/design/**`, `docs/development/**`
- 내용: 번호 기반 문서 폴더를 역할이 드러나는 이름으로 변경했다.
- 이유: 폴더 이름만 보고 문서의 목적을 이해할 수 있도록 하기 위해서다.

- 행동: 이동
- 파일: `previews/**` → `docs/design/previews/**`
- 내용: 검토용 HTML 시안을 디자인 산출물 아래로 이동했다.
- 이유: 시안이 실제 Next.js 소스가 아니라 승인용 디자인 산출물임을 명확히 하기 위해서다.

- 행동: 이동
- 파일: `eslint.config.mjs`, `prettier.config.mjs`, `vitest.config.ts`, `lint-staged.config.mjs`, `.prettierignore` → `config/**`
- 내용: 이동 가능한 개발 도구 설정을 `config/` 폴더로 모았다.
- 이유: 루트에 노출되는 설정 파일 수를 줄이기 위해서다.

- 검증: 모든 이동 대상과 목적지가 저장소 `C:\Users\DaL\Documents\GK` 내부인지 확인한 후 이동했다. import와 도구 경로 수정 및 전체 검증은 이어서 진행한다.

## 이동된 소스와 도구 설정 연결

- 행동: 수정
- 파일: `src/app/cases/page.tsx`, `src/app/products/page.tsx`, `src/app/page.tsx`, `src/features/home/components/*.tsx`, `src/features/cases/components/CaseList.tsx`
- 내용: 기능별로 이동한 사례·제품·홈 컴포넌트와 사례 상태 파일의 import 경로를 새 `src/features/` 구조에 맞췄다.
- 이유: 폴더 이동 후에도 각 라우트와 기능 컴포넌트가 같은 화면을 조립하도록 하기 위해서다.

- 행동: 수정
- 파일: `src/features/cases/components/CaseList.tsx`, `src/features/products/components/ProductList.tsx`
- 내용: 변경되지 않는 사례 JSON을 Zustand 콘텐츠 스토어에서 가져오지 않고 `src/content/cases.json`에서 직접 가져오도록 변경했다.
- 이유: 정적 JSON을 불필요하게 전역 상태로 복사하지 않고 데이터 출처를 명확하게 하기 위해서다.

- 행동: 삭제
- 파일: `src/features/cases/store/contentStore.ts`
- 내용: 정적 JSON만 복사하던 콘텐츠 전역 스토어를 제거했다.
- 이유: 사용자 요청과 리뷰 결과에 따라 변경 상태가 아닌 정적 콘텐츠에 Zustand를 사용하지 않기 위해서다.

- 행동: 수정
- 파일: `tsconfig.json`, `config/vitest.config.ts`
- 내용: `@/` 경로 별칭이 저장소 루트 대신 `src/`를 가리키도록 TypeScript와 Vitest 설정을 함께 변경했다.
- 이유: 제품 소스를 `src/`로 이동한 뒤 앱과 테스트에서 같은 import 경로를 유지하기 위해서다.

- 행동: 수정
- 파일: `package.json`, `.husky/pre-commit`, `config/eslint.config.mjs`, `config/lint-staged.config.mjs`, `config/prettierignore`
- 내용: 이동한 ESLint·Prettier·Vitest·lint-staged 설정 경로를 각 npm 명령과 커밋 전 검사에 명시하고 생성물 제외 범위를 보완했다.
- 이유: 설정 파일을 `config/`로 옮긴 뒤에도 개발·검증 명령이 기존과 동일하게 동작하도록 하기 위해서다.

- 행동: 수정
- 파일: `AGENTS.md`, `README.md`
- 내용: 역할, 변경 기록, 디자인 시안, 콘텐츠, 개발 문서의 새 경로를 안내하도록 현재 운영 경로를 변경했다.
- 이유: 사람과 AI가 이동 전 경로를 따라가지 않도록 프로젝트 진입 문서를 최신 구조와 일치시키기 위해서다.

- 검증: import와 문서 내부의 나머지 이전 경로를 전체 검색한 뒤 추가 수정하고, 테스트·린트·빌드를 실행할 예정이다.

## 디자인 시안 경로 단순화

- 행동: 이동
- 파일: `docs/design/previews/design/**` → `docs/design/previews/**`
- 내용: PC·모바일 시안과 디자인 비교 HTML을 검토용 시안 폴더 바로 아래로 이동했다.
- 이유: `design/previews/design`처럼 의미가 중복되는 폴더 단계를 없애고 승인 시안을 한 경로에서 찾게 하기 위해서다.

- 행동: 삭제
- 파일: 빈 `components/`, `docs/design/previews/design/`
- 내용: 파일 이동 후 남은 빈 이전 폴더를 제거했다.
- 이유: 루트와 시안 폴더에 더 이상 사용하지 않는 이전 구조가 표시되지 않게 하기 위해서다.

## 운영 문서 경로 갱신

- 행동: 수정
- 파일: `AGENTS.md`, `README.md`, `docs/**`, `harness/roles/**`, `harness/review/**`
- 내용: `docs/01-planning`, `docs/02-design`, `docs/03-development`, `docs/04-review`, `previews`, `roles`, `patchNote`를 가리키던 현재 문서 경로를 새 `docs/`와 `harness/` 구조로 일괄 변경했다.
- 이유: AI와 개발자가 이동 전 경로를 읽어 존재하지 않는 파일을 찾거나 잘못된 위치에 산출물을 만들지 않게 하기 위해서다.
- 검증: 일괄 변경 중 발견한 `harness/harness`, `docs/design/docs/design` 중복 경로를 즉시 정정하고 해당 중복 문자열이 남지 않았음을 검색으로 확인했다.

## 개발 구조 문서와 결정 동기화

- 행동: 수정
- 파일: `docs/development/project-structure.md`
- 내용: 현재 루트, `src/app`, 기능별 `src/features`, 공통 컴포넌트, 콘텐츠, 문서, 하네스, 설정 구조와 각 책임을 실제 파일 기준으로 다시 작성했다.
- 이유: 주니어 개발자와 AI가 이전 루트 구조나 존재하지 않는 파일을 기준으로 코드를 찾지 않도록 하기 위해서다.

- 행동: 수정
- 파일: `docs/requirements/implementation-decisions.md`
- 내용: `src/` 경로 별칭, 기능별 폴더, 정적 JSON 직접 import, 사례 상태의 기능 내부 관리 방식으로 관련 결정을 갱신했다.
- 이유: 제거한 `contentStore`와 이동 전 경로를 확정 결정으로 계속 안내하는 모순을 없애기 위해서다.

- 행동: 수정
- 파일: `docs/development/code-style.md`
- 내용: 공통 타입과 기능 전용 타입의 위치를 구분하고, 정적 JSON에는 Zustand를 사용하지 않는 상태 관리 기준을 반영했다.
- 이유: 데이터와 상태의 역할을 코드 스타일 단계에서 명확하게 제한하기 위해서다.

- 행동: 삭제
- 파일: 이동 후 비어 있지 않은 폴더에 남아 있던 `.gitkeep` 13개와 루트 `components/.gitkeep`
- 내용: 실제 파일이 존재해 더 이상 필요하지 않은 빈 폴더 유지 파일을 제거했다.
- 이유: 이전 루트 `components/`를 제거하고 제품 소스에 의미 없는 파일이 표시되지 않게 하기 위해서다.

## 문서의 제품 소스·설정 경로 갱신

- 행동: 수정
- 파일: `docs/**`, `harness/roles/**`, `harness/review/**`
- 내용: 루트의 `app`, `components`, `content`, `lib`, `types`와 개별 도구 설정 파일을 가리키던 설명을 현재 `src/`와 `config/` 경로로 변경했다.
- 이유: 문서와 리뷰 기록에서 클릭하거나 검색하는 경로가 이동된 실제 파일을 가리키도록 하기 위해서다.
- 검증: `src/src`, `config/config`처럼 중복된 경로가 없음을 검색으로 확인했다. 남은 `store/` 표기는 현재 구조 설명과 별도 수정이 필요한 의존성·옛 시안 설명뿐이다.

## 하네스와 구조 안내 보완

- 행동: 수정
- 파일: `docs/development/dependencies.md`, `docs/design/previews/dependency-setup.html`
- 내용: React·Zustand의 실제 사용 위치와 새 `src`, `config`, `docs`, `harness` 구조를 현재 경로로 정정했다. 경로 치환 중 잘못 바뀐 npm 패키지명 `@types/*` 표기도 즉시 복구했다.
- 이유: 의존성 이름과 사용 위치가 실제 `package.json` 및 소스 구조와 일치하도록 하기 위해서다.

- 행동: 수정
- 파일: `harness/roles/developer.md`
- 내용: 개발 전에 관련 요구사항, 디자인 시스템, PC·모바일 승인 시안과 수정 파일의 대응 관계를 확인·보고하고, 충돌이나 데이터 누락은 사용자에게 질문하도록 절차를 추가했다.
- 이유: 개발 역할 AI가 기존 디자인을 읽지 않고 임의 화면을 구현하는 문제를 예방하기 위해서다.

- 행동: 수정
- 파일: `AGENTS.md`
- 내용: `src`, `config`, `docs`, `harness`의 책임과 자동 생성 폴더 관리 규칙을 공통 규칙에 추가했다.
- 이유: 저장소에 진입한 사람과 AI가 최상위 폴더의 용도를 먼저 구분할 수 있도록 하기 위해서다.

## 코드·설정 서식 정규화

- 행동: 수정
- 파일: `config/**`, `src/**`, `tests/cases.test.ts`, `package.json`, `tsconfig.json`, `next.config.ts`, `next-env.d.ts`, `postcss.config.mjs`
- 내용: 프로젝트에 설정된 Prettier를 실행해 이동·수정된 코드와 설정을 LF, 공백 2칸, import·Tailwind 클래스 순서 규칙에 맞췄다.
- 이유: `npm run format:check`에서 40개 파일의 기존 줄바꿈 및 서식 차이가 확인되어 새 위치에서도 커밋 전 검사가 통과하도록 하기 위해서다.
- 검증: `npm run format` 완료. 파일 내용의 동작은 변경하지 않았다.

## 최종 검증

- 행동: 검증
- 파일: 전체 프로젝트
- 내용: 새 폴더 구조의 import, TypeScript 경로 별칭, 개발 도구 설정 경로와 Next.js 정적 빌드를 확인했다.
- 이유: 파일 이동 뒤 개발·검사·빌드 흐름이 깨지지 않았음을 확인하기 위해서다.
- 검증: `npm test` 1개 파일·4개 테스트 통과, `npm run lint` 통과, `npm run format:check` 통과, `npm run build` 통과. 정적 라우트 `/`, `/cases`, `/company`, `/products`, `/_not-found` 생성 확인. `git diff --check` 오류 없음.

- 행동: 삭제
- 파일: `.next/**`, `out/**`
- 내용: 빌드 검증으로 생성된 Next.js 내부 결과와 정적 배포 결과를 검증 완료 후 제거했다.
- 이유: 자동 생성물이 정리된 루트 폴더 구조에 계속 표시되지 않도록 하기 위해서다.
- 복구: `npm run dev` 또는 `npm run build`를 실행하면 필요한 폴더가 자동으로 다시 생성된다.

# 미해결 이슈 목록

이 파일은 현재 해결되지 않은 이슈만 빠르게 찾기 위한 중앙 목록이다. 이슈가 해결되면 이 파일에서 항목을 제거하고, 최초 기록 파일에 해결 방법과 검증 결과를 남긴다.

## 26-08-12-dev-issues

- `DEV-REV-005` · 높음 · 포커스 이동·Tab 제한·닫은 뒤 복귀를 구현했지만 실제 키보드 수동 검증이 남아 있다.
- `DEV-REV-015` · 보통 · 필수 SEO 산출물인 `robots.ts`와 `sitemap.ts`가 없어 검색 로봇 안내와 사이트 URL 목록을 제공하지 못한다.
- `DEV-REV-016` · 보통 · 홈·회사 소개의 라우트 파일이 화면 구현을 직접 맡는다. 확정한 홈 3개·회사 소개 4개 자식 화면으로 분리해야 한다.
- `DEV-REV-017` · 높음 · 사례 목록의 데이터·URL·상태·화면이 `CaseList`에 집중돼 있다. 사례 페이지와 자식 화면, `useCaseList` 하나로 기능을 옮기고 기존 집중 파일을 제거해야 한다.
- `DEV-REV-018` · 보통 · 제품 목록의 데이터·선택 상태·화면이 `ProductList`에 집중돼 있다. 제품 페이지와 자식 화면, `useProductList`로 나누고 선택 제품은 `gk-product-list`에 저장해 새로고침·페이지 이동 뒤에도 유지해야 한다.
- `DEV-REV-019` · 보통 · `CaseDetailModal`이 모달 접근성·스크롤 잠금·사진 갤러리·확대 뷰어·상세 화면을 한 파일에서 처리한다.
- `DEV-REV-020` · 보통 · 필수 전화번호를 타입·콘텐츠 검사·모든 화면에서 같은 기준으로 사용하도록 통일해야 한다.
- `DEV-REV-021` · 높음 · 사례 필터·페이지는 `gk-case-list`에 저장해 모든 새로고침·페이지 이동 뒤에도 유지하고, 사용자가 필터 초기화를 눌렀을 때만 지워야 한다.
- `DEV-REV-022` · 보통 · 사례 목록의 장소·자재 태그 변경 처리에 같은 선택 교체 규칙이 중복돼 있어 사례 목록 전용 파일의 한 동작으로 모아야 한다.
- `DEV-REV-023` · 보통 · 사례·제품·회사 소개의 같은 첫 영역은 `PageHero` 하나로 만들고 각 페이지 문구만 전달해야 한다. 홈 히어로는 별도 화면으로 유지한다.
- `DEV-REV-024` · 높음 · 홈과 제품 목록의 제품 카드는 `ProductCard` 하나를 직접 재사용하고, 화면별 그리드만 다르게 해야 한다.
- `DEV-REV-025` · 높음 · 확정된 함수·타입 한 줄 주석 규칙이 `src`의 TS/TSX 코드에 적용되지 않았다.
- `DEV-REV-027` · 높음 · 사례 상세에서 합의한 부모 조립·자식 화면·자식의 직접 기능 import·props 기반 공용 화면 재사용 규칙을 `src` 전체 구조에 적용해야 한다.

# 미해결 이슈 목록

이 파일은 현재 해결되지 않은 이슈만 빠르게 찾기 위한 중앙 목록이다. 이슈가 해결되면 이 파일에서 항목을 제거하고, 최초 기록 파일에 해결 방법과 검증 결과를 남긴다.

## 26-08-12-dev-issues

- `DEV-REV-004` · 높음 · 제품 필터가 승인된 다중 AND 선택 규칙과 다르게 한 제품만 선택하고 별도 초기화 버튼을 표시한다.
- `DEV-REV-005` · 높음 · 사례 상세 팝업과 사진 뷰어에 키보드 초점 이동 제한·닫은 뒤 원래 요소로 초점 복귀가 없다.
- `DEV-REV-006` · 보통 · 페이지 조립용 `<main>` 안에 기능 컴포넌트가 다시 `<main>`을 만들어 문서 랜드마크가 중복된다.
- `DEV-REV-007` · 보통 · `CaseList`가 URL 읽기, 상태 변경, 필터 계산, 페이지 이동, 팝업 제어와 화면 표시를 함께 맡아 수정 범위를 파악하기 어렵다.
- `DEV-REV-008` · 보통 · 정적 JSON의 필수값·참조·형식을 확인하는 콘텐츠 검사 명령이 없어 콘텐츠 추가 시 오류를 조기에 발견하기 어렵다.
- `DEV-REV-009` · 낮음 · 자동 테스트가 현재 JSON의 특정 ID와 개수에 묶여 있어 정상적인 콘텐츠 추가·교체 때 함께 수정해야 한다.
- `DEV-REV-010` · 보통 · `getProductCase`가 홈 전용 모듈과 공통 제품 모듈에 중복되어 제품 조회 규칙의 수정 위치가 둘로 나뉜다.
- `DEV-REV-011` · 보통 · 회사 소개 문구가 `paragraphs[0]`처럼 배열 위치에 의존해 콘텐츠 순서 변경에 취약하다.
- `DEV-REV-012` · 보통 · 홈 대표 제품 3개의 선정 규칙이 사례 JSON의 순서에 암묵적으로 의존한다.
- `DEV-REV-013` · 높음 · `phone: null`일 때 Header와 MobileMenu이 `replaceAll()`을 호출해 화면 렌더링이 실패할 수 있다.
- `DEV-REV-014` · 보통 · 음수 페이지 번호가 목록 계산과 Zustand 상태에 들어오는 것을 막지 못한다.
- `DEV-REV-015` · 보통 · 필수 SEO 산출물인 `robots.ts`와 `sitemap.ts`가 없어 검색 로봇 안내와 사이트 URL 목록을 제공하지 못한다.
